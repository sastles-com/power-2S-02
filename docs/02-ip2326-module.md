# 02 — IP2326 充電回路のオンボード化 (2S 昇圧充電)

<subproject>
- name: ip2326-module
- parent: power-2s-02
- status: **確定 (2026-08-10)** — 入手性・データシート照合・全定数を決定。**作図に着手できる**
- depends_on: [01-legacy-analysis]
</subproject>

## Scope

既製品「Type-C 15W 2-3S Lithium Battery Charging boost Module」(IP2326 搭載) の充電回路を
新基板へディスクリート実装するための設計指針。

## Out of scope

- バッテリ保護 (XR-2S-30A オフボード、CLAUDE.md 参照)
- 5V 降圧 ([`docs/03`](03-mp1584-module.md))

## 0. 入手性 (2026-08-10 確認 — Q2 クローズ)

<confirmed>
**IP2326 は入手可能。代替 IC の検討は不要。**

| 項目 | 値 |
| --- | --- |
| **選定品** | **IP2326 (標準品)** / INJOINIC (英集芯) |
| **LCSC** | **C2832094** |
| パッケージ | **VQFN-24-EP (4×4 mm, 0.5 mm ピッチ, EPAD あり)** |
| LCSC 在庫 | 23,110 pcs |
| LCSC 価格 | $0.62 (1+) / $0.40 (100+) / $0.30 (500+) |
| EOL / NRND | **なし** |
| **JLCPCB 実装** | **可 (SMT Assembly, Economic & Standard 対応)**。ライブラリ区分 **Extended (expand)**、在庫 23,110 |
| MSL | 3 (実装前ベーク要否は JLCPCB 側で管理) |

### ⚠️ バリアントの選択 — `_NPD` を選んではいけない

データシート §8 "Type Description" に 3 品種が定義されている:

| 品種 | 内容 | 本プロジェクトでの採否 |
| --- | --- | --- |
| **IP2326** | 標準品。VSET / ISET / UVSET 等をすべてサポート | **✅ これを使う** (C2832094) |
| IP2326_8V8 | 標準品比で CV 充電電圧が +0.4 V | ✗ 2S LiPo に 8.8 V は過充電 |
| IP2326_NPD | **充電電流設定が 700 mA 未満**の用途向け | ✗ 目標 1.0 A に届かない |

LCSC には `IP2326_NPD` (**C5441281**, 4,669 pcs, $0.34@100) も在庫があり、型番が似ているため
発注時に取り違える危険がある。**BOM には必ず C2832094 と書く。**

(`NPD` は「PD 非対応」ではなく、データシートの定義どおり**低充電電流品**。
なお本設計は Type-C を載せないので PD/QC 交渉自体を使わない → §3.3)
</confirmed>

## 1. 確定事項

<confirmed>
- **IC: IP2326** (LCSC C2832094)。5V 入力から 2S (8.4 V) へ**昇圧充電**、500 kHz 同期整流
- **2S 構成** — **CON_SEL (pin 10) を未接続 (floating)** にすることで 2S を選択 (§3.1)
- **BM (セル中点) を配線** — バランス端子は **BMS を経由せずセル中点へ** (legacy 踏襲、Hard Rule)。
  データシート要求の直列抵抗は入れる。**RCB = 120 Ω / 1206** (標準回路の 100 Ω は採らない、§3.2)
- **Type-C レセプタクルは載せない** (ユーザー確認 2026-08-10) — 充電入力は**コネクタのみ**
  (南極の磁気端子 → AWG26×2 → 本基板)。したがって **CC 抵抗 (5.1 kΩ×2) は不要**、
  **DM/DP (pin 1,2) は未接続** (§3.3)
- **NTC 機能は使わない** (ユーザー確認 2026-08-10) — **RNTC 51 kΩ で GND へ**。サーミスタ非実装 (§3.6)
- **設計の根拠: データシート標準アプリケーション回路 (Figure 5) + 自前計算**
- **保護機能は使わない** — 過充放電/過電流は XR-2S-30A の役割 (Hard Rule)。
  ただし IC 内蔵の**入力 UV/OV・充電タイムアウト・過温度**は無効化できないので設定値だけ決める (§3.4)
- **充電状態 LED は実現可能** — LED pin (6) が専用出力として存在する (§3.5)。
  → [`docs/06`](06-power-switch.md) の「充電中 LED」要件のブロッカーは解消
</confirmed>

### データシート情報源

- **一次資料: ChipSourceTek 版 IP2326 データシート V1.2 (19 ページ, 英語)**
  - IC 自体は INJOINIC (英集芯) 品。ChipSourceTek は同一ダイの販売元で、英語版が入手できるのはこちら
  - 入手 URL: `https://done.land/assets/files/ip2326_datasheet.pdf`
  - LCSC 添付 PDF (`lcsc.com/datasheet/C2832094.pdf`) は同内容だが直リンクがブロックされる
- 補足解説 (実機検証ブログ): `https://done.land/components/power/powersupplies/battery/chargers/charge/powermanagementics/ip2326/`
  - ⚠️ ここには「2S は 180 kΩ、3S ははんだブリッジ」という**特定モジュール基板固有**の記述がある。
    **IC そのものの仕様はデータシートが正** (2S = floating / 3S = 1 kΩ to GND)

### legacy 基板での使われ方 ([`docs/01`](01-legacy-analysis.md))

- VIN ← 充電入力 (D1 ショットキー後)。モジュールの Type-C は使わず VIN 穴給電だった
- B+ → P+ (BMS 保護後)、B− → PGND、**BM → セル中点**

## 2. ピン配置 (VQFN-24-EP 4×4、データシート Figure 2 / §4)

| Pin | 名称 | 機能 | 本設計での扱い |
| --- | --- | --- | --- |
| 1 | DM | USB D− (入力急速充電の要求に使用) | **未接続 (NC)** — 配線を引かない (§3.3) |
| 2 | DP | USB D+ (同上) | **未接続 (NC)** — 配線を引かない (§3.3) |
| 3 | VSET | CV 充電電圧の微調整 | **NC → 8.4 V** (§3.1) |
| 4 | NTC | 電池温度検出。20 µA 定電流を吐き出す | **51 kΩ 1% で GND へ** (不使用処理、§3.6) |
| 5 | BAT_STAT | 充電状態デジタル出力。トリクル中 = L、CC 以降 = H | 将来の ESP32 通知用に**パッド予約** |
| 6 | LED | 充電表示 LED 駆動 (最大 5 mA) | **LED + 100 Ω** (§3.5) |
| 7 | TIME_SET | 充電タイムアウト設定 (ROT) | **NC → 24 h** (§3.4) |
| 8 | VIN_UVSET | 入力低電圧閾値設定 (RUV) | **68 kΩ → 4.35 V** (§3.4、要検討) |
| 9 | VIN_OVSET | 入力過電圧閾値設定 (ROV) | **NC → 8.75 V** (§3.4) |
| 10 | CON_SEL | セル数選択。**floating = 2S** / 1 kΩ to GND = 3S | **NC (未実装)** (§3.1) |
| 11 | ISET | 充電電流設定。**floating 禁止** | **91 kΩ 1% → 0.99 A** (§3.1) |
| 12 | EN | イネーブル。L で停止 (H ≥1.4 V / L ≤1.2 V) | **VIN へプルアップ** (§3.7) |
| 13 | VIN | 入力電源 + 入力電圧検出ピン | **R1 1 Ω + C3 10 µF** (§3.8) |
| 14 | BST | ブートストラップ。0.1 µF を BST–LX 間に IC 直近で配置 | 0.1 µF (0603) |
| 15,16,17 | LX | DCDC スイッチノード → 外付けインダクタ | L1 2.2 µH (§3.9) |
| 18 | PGND | パワーグランド | GND ベタ |
| 19,20 | VSYS | 昇圧中間ノード。**22 µF セラミック×2 をピン直近**に | 22 µF/25 V ×2 |
| 21,22 | VOUT | 昇圧出力 → バッテリ + | B+ (BMS 経由 P+ へ、[`docs/01`](01-legacy-analysis.md)) |
| 23 | VBATM | バランス用セル中点検出 + 均等化シャント経路。未使用時は floating | **RCB 120 Ω 経由で BM へ** (§3.2) |
| 24 | VBAT_GND | バランス用の電池 − 検出 | **B− へ直結** (RCB は VBATM 側に 1 個だけ、§3.2) |
| EPAD | GND | パワーグランド + 放熱 | **サーマルビア → 内層/Bottom ベタ** ([`docs/04`](04-layout-thermal.md)) |

## 3. 定数設計 (データシート Figure 5 + 自前計算)

### 3.1 セル数 / 充電電圧 / 充電電流

| 設定 | ピン | 選択値 | 結果 |
| --- | --- | --- | --- |
| セル数 | CON_SEL (10) | **未実装 (floating)** | **2S** |
| CV 電圧 | VSET (3) | **未実装 (NC)** | **8.4 V** (= 4.20 V/cell、2S LiPo 標準) |
| 充電電流 | ISET (11) | **91 kΩ 1% (LCSC C23265)** | **0.99 A** |

⚠️ **RISET は 90 kΩ ではなく 91 kΩ を使う** (2026-08-11 修正)。
データシートの表は 90 kΩ → 1.0 A と書いているが、**90 kΩ は E24 / E96 いずれにも存在しない標準外の値**。
E24 の **91 kΩ** なら `ICHG = 90000/91000 = 0.99 A` で設計意図と実質同じ。
以下の計算は 1.0 A のまま読んでよい (差は 1%)。

- CV 電圧は RVSET で 8.1 / 8.2 / 8.3 / 8.4 V (1 k / 68 k / 120 k / NC) から選べる。
  寿命重視で 8.2 V に落とす選択もあるが、容量が ~10% 減る。**まず 8.4 V (NC) で設計**
- 充電電流は **ICHG = 90000 / RISET** (RISET は Ω)。**ISET は floating 禁止**、精度 1% 品を使う

  | RISET | ICHG | 8.4 V 出力時の電力 | 効率 92% での 5 V 入力電流 | AWG26 (~2 A) 判定 |
  | --- | --- | --- | --- | --- |
  | 120 kΩ | 0.75 A | 6.3 W | 1.37 A | ◎ 余裕 |
  | **90 kΩ** | **1.0 A** | **8.4 W** | **1.83 A** | **○ 上限内** |
  | 75 kΩ | 1.2 A | 10.1 W | 2.19 A | ✗ 超過 |
  | 60 kΩ | 1.5 A | 12.6 W | 2.74 A | ✗ 大幅超過 |

  ※ IC 自身の消費 (EN=H で 20 mA typ) を足すと 90 kΩ でも入力 ~1.85 A。上表は効率 92% (2S/VIN=5V の
  データシート実測カーブの保守側) で計算。

#### 充電電流の推奨値 → **0.99 A (RISET = 91 kΩ, LCSC C23265)**

- **律速は IC ではなく充電入力の配線** — 親 `docs/03-power-charging.md` Q41 が指摘する
  **AWG26 の安全電流 ~2 A** が上限を決める。IC 側の上限 1.5 A は使い切れない
- 2000 mAh に対し **0.5 C**。CC 領域 + CV 領域で満充電まで **約 2.5〜3 時間** (実用的)
- 15 W 級を謳うモジュールだが、本設計の実効は **~8.4 W (5 V×1.83 A 入力)**。
  これは Type-C を載せず PD/QC で 9 V を引けないこと (§3.3) とも整合する
- なお入力電圧が RUV 閾値に近づくと IC が**自動的に充電電流を絞る**ため、
  ケーブル電圧降下で電源が落ちることはない (安全側に転ぶ)

### 3.2 バランス (充電均等化) 回路 — **RCB = 120 Ω / 1206 で確定** (2026-08-10)

データシート "Charge Equalization Function" より:

- 充電中、片方のセルが **VCBON = 4.1 V** に達すると**そのセル側の内蔵均等化 MOS を ON** し、
  余った充電電流をシャントしてそのセルの実効充電電流を減らす
- 均等化電流 **ICB = VCB / RCB**。制約は **RCB ≥ 100 Ω** かつ **ICB < 40 mA**
  (電力は内蔵 MOS と RCB で熱になる)
- 標準回路 (Figure 5): **RCB は VBATM (23) のラインに 1 個**、VBATM / VBAT_GND に **0.1 µF (C7/C8)**
- 未使用なら pin 23/24 を floating → **本設計は使う** (2S のセルバランスは必須)

#### RCB が 1 個で足りる理由 (回路動作の確認)

シャント経路は 2 本あるが、**どちらも VBATM (pin 23) を通る**ので RCB 1 個が両方に直列に入る:

- 上セル側: `VOUT (=B+) → 内蔵 MOS → pin 23 → RCB → BM`
- 下セル側: `BM → RCB → pin 23 → 内蔵 MOS → pin 24 (=B−)`

したがって **BM ライン (セル中点) に RCB を 1 個直列**に入れるのが標準回路であり、
これは省略できない。**RCB を 0 Ω にすると内蔵 MOS が Rds(on) だけでセルを短絡し、IC が壊れる。**

#### 値の決定: 100 Ω ではなく **120 Ω** を選ぶ

データシートの 2 つの制約は **RCB = 100 Ω では両立しない**:

| RCB | ICB = 4.1 V / RCB | `ICB < 40 mA` 判定 | RCB の消費電力 |
| --- | --- | --- | --- |
| 100 Ω (標準回路の記載値) | **41 mA** | **✗ わずかに超過** | 0.17 W |
| **120 Ω (採用)** | **34 mA** | **○** | **0.14 W** |
| 150 Ω | 27 mA | ○ | 0.11 W |

- 標準回路の 100 Ω は「RCB ≥ 100 Ω」の下限ちょうどで、VCBON = 4.1 V を代入すると
  もう一方の制約 (ICB < 40 mA) を破る。**E24 の一段上 = 120 Ω** が両方を満たす最小値
- 均等化能力は ICB が大きいほど強いので、**制約内で最大に近い 120 Ω** が最良の選択
  (150 Ω 以上にすると均等化が無駄に弱くなる)
- 41 mA → 34 mA の差は均等化能力で 20% 弱の低下にすぎない。密閉された球体内の QFN パッケージで
  データシート上限を超える運用をする方がリスクが大きい
- **パッケージは 1206** — 0.14 W に対し 0805 (0.125 W) では定格を割る。1206 (0.25 W) で余裕を取る

#### 均等化能力の評価 (系全体での位置づけ)

- ICB 34 mA は充電電流 1.0 A の **3.4%**。3 時間の充電で **約 100 mAh 分**のセル差を詰められる
  (2000 mAh に対し 5%) — 弱いが実用範囲
- **系内で能動バランスを行うのは IP2326 だけ**。XR-2S-30A はセル保護が主目的でバランス機能は期待しない
  → この回路を省略・弱体化させない
- BM 配線に流れるのは最大 34 mA。**ワイヤ径の制約にはならない** ([`docs/01`](01-legacy-analysis.md) のコネクタ選定に影響なし)

#### Hard Rule 「BM をセル中点へ直結」との整合 (2026-08-10 決着)

この「直結」は **「BMS を経由せずセル中点へ引く」**という意味 (legacy 踏襲、[`docs/01`](01-legacy-analysis.md) §3)。
**抵抗ゼロで結ぶことを要求するルールではない** → **RCB 120 Ω の直列挿入は入れる**。
CLAUDE.md §3 の Hard Rule に注記済み。

### 3.3 DM / DP と入力急速充電 — **使わない。未接続 (NC) で確定** (2026-08-10)

<confirmed>
**Type-C レセプタクルは載せない** (ユーザー確認 2026-08-10)。したがって:
- **DM / DP (pin 1,2) は未接続 (NC)** — 配線を引かず、パッドで止める
- **CC1 / CC2 の 5.1 kΩ×2 も不要** (標準回路 Figure 5 の R3/R5)
</confirmed>

- IP2326 は電池電圧に応じて **DP/DM 経由で 5.4 / 6 / 7 V の入力を要求**する
  (2S 時: 電池 6.2 V 未満は 5 V のまま / 6.2–6.8 V→5.4 V / 6.8–7.8 V→6 V / 7.8 V 超→7 V)
- **要求に失敗した場合は常に 5 V 入力で充電する**とデータシートに明記されている
  → **未接続でも機能欠落や誤動作にはならない。** 磁気端子から素の 5 V を受けるだけの本設計に適合する
- 副作用: 昇圧比が常に 5 V→8.4 V となり、効率は 9 V 入力時 (~94%) より低い **~92%**。
  §3.1 の充電電流計算はこれを前提にしている

#### 未接続の処理方法 (データシートに記載がないため方針を明記)

データシートは未使用時の処理を書いていない。DM/DP は QC 交渉のために**電圧を出力する側**の
ピンであり、高インピーダンスのゲート入力ではないので **floating で問題は起きないと判断**する。
方針:

- **配線を引かない** (パッドのみ)。500 kHz のスイッチノード (LX) が近いので、
  余分な配線を伸ばしてアンテナを作る方が有害
- 保険として **各ピン → GND の 100 kΩ を「未実装 (DNP)」フットプリントで 1 対だけ IC 直近に予約**する。
  実装しないのでコストは 0。万一 floating が原因の誤動作が出たら実装で対処できる
- ⚠️ ただし 40×40 mm はスペースが厳しい ([`docs/04`](04-layout-thermal.md))。
  **レイアウトが詰まった場合は DNP パッドを省略してよい** (優先度は低い)

### 3.4 保護閾値の設定 (内蔵機能は無効化できないので値だけ決める)

| 設定 | ピン | 選択 | 閾値 | 根拠 |
| --- | --- | --- | --- | --- |
| 入力 UV | VIN_UVSET (8) | **68 kΩ** | **4.35 V** | 磁気端子 + AWG26×2 の電圧降下を許容する。既定 NC (4.65 V) だと 1.8 A 通電時のケーブル降下で IC が充電電流を絞ってしまう |
| 入力 OV | VIN_OVSET (9) | **NC** | 8.75 V | 入力は 5 V 固定 (PD 交渉なし)。既定で十分 |
| タイムアウト | TIME_SET (7) | **NC** | 24 h | 満充電まで ~3 h。4 h (68 k) は CV 領域で誤トリップの恐れ |

- 過温度保護 (135 °C shutdown / 110 °C 復帰) と出力 OC/短絡保護は固定機能
- **これらは Hard Rule 違反ではない** — 電池側の過充放電/過電流保護 (XR-2S-30A の役割) とは別物で、
  IC の自己保護と入力側保護。**電池保護を IP2326 に依存しない**方針は維持

### 3.5 充電状態 LED — **pin 6 は「ソース (吐き出し)」で確定 (2026-08-11 図で検証)**

<confirmed>
**データシート Figure 5 (p.16 "IP2326 Typical Application Schematic") を画像でレンダリングして確認した結果:**

```
pin 6 (LED) ──[R2 100Ω]──▶|── GND
                          LED (アノード上 / カソード GND)
```

**電流は pin 6 から出ていく = ソース出力。** プルアップは無い。

⚠️ **これは [`docs/08`](08-north-status-window.md) §7 Q2 の「シンク」という結論を覆す。**
同 Q2 は本 doc の「標準回路は LED + 100 Ω を GND へ」という記述をシンクと読み違えていた
(pin → LED → R → GND はソース構成である)。**pin 6 = ソースが正。**
</confirmed>

- **LED pin (6)** — 専用出力。**ソース。最大駆動 5 mA**。標準回路は **pin6 → R2 100 Ω → LED → GND**
- 動作: **充電中 = 点灯 / 満充電 = 消灯 / 異常検出時 = 点滅**
  → [`docs/06`](06-power-switch.md) Q4 の「充電中/満充電を区別するか」に対し、**IC 側が無料で区別してくれる**
- ⚠️ **pin 6 の出力電圧はデータシートに規定が無い。** 規定されているのは
  `ILed` の **max 5 mA のみ** (BOM 注記は "LED indicator, maximum drive capacity 5mA")。
  100 Ω で 5 mA を超えない以上、**pin 6 の compliance は 2.5 V 前後か、内部で 5 mA に自己制限している**
  と読むしかない。**したがって抵抗はメーカー指定の 100 Ω をそのまま使う** — 出力電圧を仮定して
  独自に値を決めると、暗すぎるか定格超過のどちらかに振れる。**実機で電流を実測すること (V15)**
- **BAT_STAT pin (5)** はデジタル出力 (トリクル中 L / CC 以降 H)。
  ESP32 へ充電状態を渡す将来拡張に使える → **パッドのみ予約** (電源スイッチのソフト電源断ペンディングと同じ扱い)

#### 電源基板から LED を降ろした (2026-08-11)

**`LED1` と `R2` は電源基板から削除した。** 理由は 2 つ:

1. **重複** — 充電表示は**北極キャリア基板へ移設済み** ([`docs/08`](08-north-status-window.md) §5.2a)。
   電源基板上の LED は球体コアの中にあり**外殻に埋まって視認できない**
2. **電流** — **pin 6 は 5 mA が上限で 2 灯を並列に点けられない**

**pin 6 は netport `CHG_LED` として `CN7` (北極 6 極コネクタ) へ渡す。**
直列抵抗 100 Ω は北極側に置く (電気的には同じで、LED と同じ基板にある方が扱いやすい)。

### 3.3a 充電器側を USB-C にする場合の検討 (2026-08-12、ユーザー質問への回答)

現状: 磁気端子の先のケーブルは **DC プラグ + 5 V AC アダプタ**。これを USB-C にできるか。

<confirmed>
**電流は足りる。ただし 2 つの制約がある:**

1. **交渉は必ずケーブル側 (球体の外) で行う** — **磁気端子は 2 極**しかないので
   CC / DP / DM は球体内に持ち込めない。基板が見るのは常に**固定 DC 電圧**
2. **2S モードで受けられる入力は最大 ~7 V** — 昇圧チャージャなので `VIN < VOUT (8.4 V)` が必須。
   IC の入力 OV 閾値も **2S では最大 8.75 V (ROV=NC)** に設定されている。
   → **USB-PD の固定電圧 (5/9/15/20 V) のうち使えるのは 5 V だけ。9 V は OVP で止まる**
</confirmed>

| 案 | ケーブル側 | 基板が受ける | 入力電流 (充電 1.0 A) | 充電電流の上限 (AWG26 ~2 A) | 判定 |
| --- | --- | --- | --- | --- | --- |
| 現状 | DC プラグ + 5 V アダプタ | 5 V | 1.83 A | ~1.0 A | — |
| **A** | **USB-C → 2 線 (交渉なし)** | **5 V** | 1.83 A | ~1.0 A | **○ 最も簡単。基板変更ゼロ** |
| **B** | **USB-C + PPS/QC3 トリガ (7 V 設定)** | **7 V** | 1.30 A | **~1.2〜1.5 A** | **◎ 充電が速くなる。要 RISET 変更** |
| C | USB-C + PD トリガ (9 V 設定) | 9 V | — | — | **✗ OVP で充電しない** (8.75 V 超) |

- **案 A**: USB-C の素の給電能力は **5 V / 3 A** で、必要な 1.83 A に対し十分。
  ケーブル側で **CC1/CC2 に 5.1 kΩ プルダウン**を付ければ 3 A 対応の充電器から素直に引ける
  (この 5.1 kΩ は**ケーブル/ドングル側**の部品。基板には載らない → §3.3 の「CC 抵抗不要」は不変)
- **案 B**: **PD の固定 9 V は使えないが、PPS (3.3〜11 V を 20 mV 刻み) または QC3.0 (200 mV 刻み) なら 7 V が作れる**。
  7 V にすると同じ電力で**入力電流が 3 割減る**ので、**AWG26 と磁気端子の接点負担が下がったうえで充電電流を上げられる**。
  1.2 A 充電なら入力 1.55 A で余裕があり、満充電 **2.0〜2.5 時間** (現状 2.5〜3 時間)
  - 必要な基板変更は **RISET の変更のみ** (91 kΩ → **75 kΩ = 1.2 A**)。RUV 4.35 V / ROV NC はそのまま使える
  - ⚠️ **トリガの設定を間違えて 9 V 以上が来ても IC は壊れない** (VIN 絶対最大 25 V、入力段の
    セラコンも 25 V 品)。ただし OVP が働いて**充電しない**だけなので、故障と区別しにくい
- **IP2326 自身の DP/DM 急速充電要求 (2S で 5.4/6/7 V) は使えない** — DP/DM が磁気端子を越えられないため。
  電圧はケーブル側で固定してもらう必要がある (§3.3 の「DM/DP は NC」は維持)
- **ROV = 1 kΩ で OV 保護を無効化して 9 V を入れる案は採ってはいけない** — 昇圧トポロジで
  `VIN > VOUT` になると入力が電池側へ素通りし、**過充電になる**。OVP はそれを防ぐためにある

**結論**: USB-C 化そのものは問題なく可能 (案 A)。ただし **USB-C にしても充電は速くならない**
— 律速はコネクタではなく AWG26 ハーネスなので、**速くしたいなら案 B (7 V) が唯一の道**。

**未決**: 案 A / 案 B のどちらにするか → [§5](#5-open-questions) Q7。案 B なら RISET を 75 kΩ に変更する。

### 3.6 NTC (電池温度監視) — **不使用で確定** (2026-08-10)

<confirmed>
**NTC 機能は使わない** (ユーザー確認 2026-08-10)。
**RNTC = 51 kΩ (0603, 1%) を NTC pin (4) から GND へ** — データシート明記の未使用処理。
サーミスタは実装しない。
</confirmed>

理由: 球体内に密閉されたパックへサーミスタ配線を追加する経路がなく、電池の熱保護は
XR-2S-30A の管轄 (Hard Rule)。IC 自身の過温度保護 (135 °C shutdown) は別途生きている。

- NTC pin (4) は **20 µA を吐き出し**、外付け抵抗に生じる電圧で温度を判定する
  - > 1.32 V: 低温 → 充電停止 / 0.56–1.32 V: 正常 / 0.43–0.56 V: やや高温 → **電流半減** / < 0.43 V: 高温 → 停止
- **51 kΩ で 20 µA × 51 kΩ = 1.02 V** → 正常範囲 (0.56〜1.32 V) のほぼ中央に落ちる
- 誤トリップ余裕の確認 (IC のピン電流は 19〜21 µA のばらつきがある):

  | ケース | NTC ピン電圧 | 正常範囲内か |
  | --- | --- | --- |
  | 21 µA × 51 kΩ×1.01 | 1.08 V | ○ (上限 1.32 V まで余裕) |
  | 標準 20 µA × 51 kΩ | 1.02 V | ○ |
  | 19 µA × 51 kΩ×0.99 | 0.96 V | ○ (下限 0.56 V まで余裕) |

  → **5% 品でも成立するが、1% 品にしておく** (コスト差がなく、電流半減モードへの誤落ちを確実に避ける)
- ⚠️ **51 kΩ を入れ忘れると NTC ピンが floating → 20 µA で電圧が上がりきり「低温」判定で充電が始まらない。**
  ERC では検出できない類の事故なので、作図時に必ず確認する
- (参考: 将来使う場合は 100 kΩ B=4100 サーミスタ + 並列 82 kΩ)

### 3.7 EN ピン (12)

- H (≥1.4 V) で動作、L (≤1.2 V) で停止。**未接続にはできない**
- 停止時の電池側消費: **EN=0 / VIN=0 で 0.7 µA typ (max 1 µA)**、VIN=5 V 印加時 2.5 µA typ
- 本設計では**常時イネーブル** (VIN からプルアップ) — 充電回路は入力があるときだけ動くのが自然で、
  [`docs/06`](06-power-switch.md) の電源スイッチ (5V 降圧側の EN 制御) とは**独立**させる。
  電源 OFF 中でも充電できる方が運用上望ましい

### 3.8 入力段 (R1 の解釈)

標準回路の **R1 = 0.5 Ω (0603)** は、電源入力ノードと **VIN pin (13)** の間に入る。
**本設計では 1 Ω (LCSC C112305) を使う** — 0.5 Ω は薄膜 1% 系 (`0603WAF…`) に存在しないため。
昇圧の主電流は **L1 経由で LX へ**流れるので、R1 に流れるのは IC 自身の消費 (~20 mA) のみ。
→ **R1 + C3 (10 µF) は VIN 検出/電源ピンの RC フィルタ**であり、電流検出シャントではない。
0603 で電力は 0.4 mW と問題にならない (1 Ω × 20 mA = **20 mV** の降下。UV 閾値 4.35 V に対し 0.5% で無害)。

- 逆流防止ショットキー (legacy D1、SS34 / LCSC C8678) は**この入力ノードの手前に**踏襲して入れる

### 3.9 外付け部品 (データシート BOM List より)

**LCSC 番号は 2026-08-11 に全件確定** ([`docs/05`](05-jlcpcb-fab.md) §2.1)。

| 部品 | 仕様 | LCSC | 備考 |
| --- | --- | --- | --- |
| **L1** | **2.2 µH / Idc 4.6 A / Isat 7.4 A / DCR 18 mΩ / 6.0×6.0 mm** (SWPA6045S2R2NT) | **C36500** | 電流検証は下記。**CD43 (4.5×4.5) より大きい**のでレイアウトで考慮 |
| C1, C3, C6 | 10 µF / **25 V** / 0805 X5R | **C15850** | C1 入力、C3 VIN ピン、C6 出力。**2P 版の C2980800 ではなくこちら** (同 MPN で Basic・耐圧明記) |
| C4, C5 | 22 µF / **25 V** / **1206** X5R | **C12891** | **VSYS (19,20) 直近に配置**。0805 の 22 µF は 16 V 品しか存在しない |
| C2 | 0.1 µF (104) / 50 V / 0603 | **C1591** | BST–LX 間、IC 直近 |
| C7, C8 | 0.1 µF (104) / 50 V / 0603 | **C1591** | バランス検出ライン (VBATM / VBAT_GND) |
| **R1** | **1 Ω** / 0603 | **C112305** | VIN ピンフィルタ (§3.8)。**0.5 Ω は薄膜 1% 系に存在しない**ので 1 Ω に変更。IC 消費 ~20 mA で降下 20 mV、UV 閾値 4.35 V に対し無害 |
| R2 | 100 Ω / 0603 | **C22775** | LED 電流制限 |
| **RCB** | **120 Ω / 1206** | **C17909** | 均等化電流 34 mA (§3.2)。**標準回路の 100 Ω は制約を破るので採らない**。1206 は 0.14 W の放熱のため |
| **RISET** | **91 kΩ / 0603 1%** | **C23265** | 充電電流 **0.99 A** (§3.1)。**90 kΩ は E24/E96 に無い標準外の値** |
| RUV | **68 kΩ / 0603** | **C23231** | 入力 UV 4.35 V (§3.4) |
| RVSET / ROV / ROT / RCON | **未実装 (NC)** | — | 8.4 V / 8.75 V / 24 h / 2S (§3.1, §3.4) |
| **RNTC** | **51 kΩ / 0603 1%** | **C23196** | **NTC 不使用処理 (§3.6)。入れ忘れると充電が始まらない** |
| D_IN | SS34 (SMA) | **C8678** | 充電入力の逆流防止 (legacy D1 踏襲) |
| D_CHG | LED 0603 赤 | **C2286** | 充電表示 (LED ピン + R2) |
| ~~R3 / R5 (CC1/CC2 5.1 kΩ)~~ | **不要** | — | Type-C を載せないため (§3.3) |
| (DNP) 100 kΩ ×2 | 未実装フットプリント | C25803 | DM/DP → GND の保険。スペース次第で省略可 (§3.3) |

#### L1 の電流検証 (2026-08-11)

データシート BOM List の「Isat / Idc ≥ 5 A」は **1.5 A 充電 / 15 W フルスペック時**の推奨値。
本設計は充電電流 0.99 A なので要求はずっと低い:

- 昇圧なのでインダクタは**入力電流**を担う: `I_L,avg = I_in = 1.83 A`
- リップル `ΔI_L = Vin × D / (L × fs) = 5 × 0.405 / (2.2 µH × 500 kHz) = 1.84 A p-p`
- **`I_L,peak = 1.83 + 0.92 = 2.75 A`**

→ C36500 (Idc 4.6 A / Isat 7.4 A) で **Idc 2.5 倍 / Isat 2.7 倍の余裕** ✓、DCR 18 mΩ は「< 20 mΩ」を満たす ✓

✅ **耐圧の確認は完了** (2026-08-11)。昇圧出力 8.4 V に対しデータシートは **25 V 品**を指定しており、
選定した **C15850 (10 µF/25 V/0805)** と **C12891 (22 µF/25 V/1206)** はいずれも 25 V で条件を満たす。
⚠️ 2P 版が使っている **C2980800 は同じ MPN だが Extended でライブラリに耐圧が入っていない**ため
**C15850 に差し替える**。0805 の 22 µF は 16 V 品までしか存在しないので 22 µF は 1206 で確定
([`docs/05`](05-jlcpcb-fab.md) §2.1)。

## 3.10 回路図の実装状況 — **CHARGE ブロック完成 (2026-08-11)**

> **Designator は 2026-08-11 にリナンバー済み** (旧 U10/L10/C12-C19/R14-R19/D10/D11)。
> 全体の対応表は [`HANDOFF.md`](../HANDOFF.md) 「Designator リナンバー」を参照。

<confirmed>
**16 部品 (`U1` `L1` `D1` `C1`〜`C8` `R1` `R3`〜`R6`) + GND netflag + NET_PORT で実装済み。
2026-08-12 に全 16 部品・55 ピンを 1 ピンずつデータシート Figure 5 と照合し、一致を確認した。**

`LED1` / `R2` (旧 LED 段) は §3.5 のとおり**削除済み**。designator に欠番 `R2` が残るのは正常。
**座標はユーザーがレイアウト中で頻繁に動く**ため、この doc には位置を書かない
(監査は `.claude/skills/eda-connect/scripts/charge-audit.js` が現在座標を読んで行う)。
</confirmed>

| ネット | ピン数 | 内訳 |
| --- | --- | --- |
| `GND` | 12 | U1.18(PGND) / U1.25(EP) / C1.2 / C2.2 / C3.1 / C4.2 / C5.2 / C7.1 / C8.1 / R4.1 / R5.1 / R6.1 |
| `CHG_VINF` | 4 | **U1.12(EN)** / U1.13(VIN) / C2.1 / R1.1 + NET_PORT |
| (LX バス) | 5 | U1.15/16/17(LX) / L1.1 / C6.1 |
| `VSYS` | 4 | U1.19/20(VSYS) / C4.1 / C5.1 |
| (入力ノード) | 4 | L1.2 / D1.1(K) / C1.1 / R1.2 |
| `BATT_P` | 3 | U1.21/22(VOUT) / C7.2 |
| `BATT_N` | 2 | U1.24(VBAT_GND) / C8.2 |
| `BATT_M` | 2 | R3.2(RCB) / C3.2 ← **C3 は `BATT_P` が正。§3.11 の残件 ②** |
| (BST) | 2 | U1.14(BST) / C6.2 |
| (VBATM) | 2 | U1.23(VBATM) / R3.1 |
| 設定抵抗 | 2 ×3 | NTC–R6 / VIN_UVSET–R5 / ISET–R4 (各 2 ピン、他端は GND) |
| `CHG_IN` | 1 | D1.2(A) — コネクタ待ち |
| `CHG_LED` | 1 | U1.6(LED) → `CN8` (北極) へ |
| `BAT_STAT` | 1 | U1.5 — パッド未実装 (§3.11 の残件 ③) |

**未接続 (NC) にしたピン**: DM / DP / VSET / TIME_SET / VIN_OVSET / CON_SEL
— いずれも §3.1〜§3.4 の設計どおり (CON_SEL floating = 2S / VSET NC = 8.4 V / ROV NC / ROT NC)。
**2026-08-12 に 6 本すべてへ NC フラグ (`pin.setState_NoConnected(true)`) を立てた** → ERC の
dangling 警告が消える。CON_SEL にテストパッドを出す場合 (§4) はこのフラグを外すこと。

### EN (pin 12) の扱い — 2026-08-12 に接続を復旧

データシート Figure 5 では **EN はパッドに引き出すだけ**で、注記は
`EN pull to high level → start work / EN pull down to ground → stop work`。
電気仕様は **EN High = min 1.4 V / max VIN** なので、**EN を VIN ノードに直結してよい** (上限ちょうど)。
本設計は §3.7 のとおり常時イネーブルなので **`CHG_VINF` (= R1 通過後の VIN ピンノード) に直結**する。

⚠️ **この接続が一度失われていた** (2026-08-12 に発見)。`CHG_VINF` の NET_PORT が VIN 側から消え、
EN 側の 1 個だけが残っていたため、**EN がどこにも繋がらない = 充電器が一切動かない**状態だった。
GUI ドラッグで NET_PORT が外れたのが原因 ([`HANDOFF.md`](../HANDOFF.md) の GUI 事故記録)。
**ネットタグが 1 個しかないネットは「宙浮き」を意味する** — 監査の第一チェック項目にすること。

⚠️ **コネクタは未配置** — 充電入力 / セル / BMS 戻りはすべて NET_PORT
(`CHG_IN` / `BATT_P` / `BATT_M` / `BATT_N`) で外に出している。
コネクタ構成が未確定のため ([`docs/01`](01-legacy-analysis.md) §4)。

## 3.11 CHARGE ブロックの残件 (2026-08-12 の監査結果)

| # | 残件 | 状態 |
| --- | --- | --- |
| ① | **`CHG_IN` / `BATT_P` / `BATT_M` / `BATT_N` / `PACK_P` の行き先コネクタ** | ✅ **実装 (2026-08-12)** — `CN1` 2P (充電入力、ユーザー配置) / `CN2` 3P (セル) / `CN3` 4P (BMS 戻り、`PACK_P`×2 + GND×2)。詳細は [`docs/01`](01-legacy-analysis.md) §4。**`CN5`/`CN6` のピン割り当てと `VBUS` の重複は未解決** (同 §6 Q3, Q4) |
| ② | **`C3` (10 µF 出力デカップリング) が `BATT_M` に乗っている** | **要修正** — データシートの C6 相当で **`BATT_P` (= VOUT) と GND の間**が正。現状は下側セルにしか効かない。ユーザーがこの一帯をレイアウト中のため未着手 |
| ③ | **`BAT_STAT` (pin 5) のパッド** | 未実装。§2 は「パッド予約」。テストポイントを 1 個出すか、コネクタに 1 本回すか未決 |
| ④ | DM/DP → GND の DNP 100 kΩ ×2 (§3.3 の保険) | **不要と判断** — NC フラグを立てたので ERC は通る。40×40 のスペース優先 |
| ⑤ | CON_SEL のテストパッド (3S 化の余地、§4) | 未実装。出す場合は NC フラグを外す |

**修復済み (2026-08-12)**: EN の `CHG_VINF` 接続 (上記)、`L1.1` ↔ LX バスの 25 mil 隙間
(GUI ドラッグで L1 が主電流経路から切れていた)。いずれも `charge-audit.js` で事後検証済み。

## 4. 新基板への実装指針

- 昇圧充電のスイッチング電流ループ (VIN → L1 → LX → 内蔵同期整流 → VSYS → VOUT) を**最小面積**でまとめる ([`docs/04`](04-layout-thermal.md))
- **VSYS の 22 µF×2 を pin 19,20 の直近**に置く (データシート明記)。BST の 0.1 µF も IC 直近
- IP2326 は発熱源。**EPAD 直下にサーマルビア → 内層/Bottom のベタ GND** へ逃がす。
  **θJA = 60 °C/W** — 損失 8% 想定で 8.4 W×0.08 ≈ 0.7 W → ΔT ≈ 42 °C。
  ベタ GND への熱結合が不十分だと 135 °C の熱シャットダウンに近づくので、ビア数は計算で裏取りする
- 充電入力は 5 V / ~1.85 A。**磁気端子と AWG26×2 の許容電流**が上限 (§3.1 で 1.0 A 充電に決定)
- CON_SEL (10) は floating にするが、**テストパッドを 1 個出して 3S 化の余地を残す**とデバッグしやすい

## 5. Open questions

**充電段の未確定事項は 2026-08-10 に全てクローズした。作図に着手できる状態。**

| 旧 Q | 内容 | 結論 |
| --- | --- | --- |
| Q1 | データシート照合 | ✅ 完了 (§2, §3) |
| Q2 | LCSC 入手性 | ✅ 入手可 — C2832094 在庫 23,110 / JLCPCB Extended で実装可 (§0) |
| Q3 | NTC を使うか | ✅ **不使用** — RNTC 51 kΩ 1% で GND へ (§3.6) |
| Q4 | 充電電流の設定値 | ✅ **0.99 A (RISET 91 kΩ 1% = C23265)** — AWG26 の ~2 A が律速 (§3.1) |
| Q5 | RCB 直列の可否 | ✅ **入れる。ただし 120 Ω** (標準回路の 100 Ω は ICB<40 mA を破る) (§3.2) |
| Q6 | DM/DP の未使用処理 | ✅ **未接続 (NC)。配線を引かない** (DNP プルダウンは NC フラグを立てたので省略、§3.11 ④) |

**Q7 (NEW, 2026-08-12): 充電器側を USB-C にするか。するなら 5 V (案 A) か 7 V PPS/QC3 (案 B) か** → §3.3a
- 案 A なら基板変更ゼロ。案 B なら **RISET を 91 kΩ → 75 kΩ (1.2 A)** に変更する
- どちらでも **磁気端子は 2 極のまま**で足りる (交渉はケーブル側)

残る依存関係 (充電段の外側):

- ~~インダクタ L1 の LCSC 品番未定~~ → ✅ **C36500 (SWPA6045S2R2NT) に確定** (2026-08-11、§3.9)。
  当初の「Isat・Idc ≥ 5 A」はデータシートの 1.5 A 充電時の推奨値で、実電流 (ピーク 2.75 A) から
  余裕 2.5 倍が確認できた。**ただし 6.0×6.0 mm で CD43 (4.5×4.5) より大きい** → レイアウト制約
- ~~セラコンの耐圧確認~~ → ✅ **完了** (2026-08-11、§3.9)。C15850 (10 µF/25 V) / C12891 (22 µF/25 V/1206)
- **セル/BMS 系コネクタの型番** — BM ラインは 34 mA しか流れないので電流制約はない ([`docs/01`](01-legacy-analysis.md) Q1)
- **EPAD のサーマルビア本数** — θJA 60 °C/W、損失 ~0.7 W ([`docs/04`](04-layout-thermal.md) で計算)
