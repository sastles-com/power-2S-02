# 09 — システム構造とボード間インターフェース

<subproject>
- name: system-structure
- parent: power-2s-02 (さらに親: Isolation Sphere V2)
- status: **draft — 未確定 5 件あり (§6)。確定した項目のみ確定として記載**
- created: 2026-08-11
- depends_on: [06-power-switch, 08-north-status-window, 親リポジトリ CLAUDE.md]
</subproject>

## 0. この doc の役割

**電源基板 (power-2S-02) から見た「何が何につながっているか」の地図。**
球体全体の設計が複数の基板に分かれてきたため、**インターフェースの取り合いを 1 箇所に集約する。**

⚠️ **確定したハード仕様 (球体寸法・LED 総数・カセット構成など) はここに書かない。**
親リポジトリの `CLAUDE.md` が正本で、ここからは参照するだけ (親 `CLAUDE.md` §8.2 の重複禁止)。

---

## 1. 基板・アセンブリの一覧

| # | 名称 | 中身 | 管轄 |
| --- | --- | --- | --- |
| **B1** | **電源基板 `power-2S-02`** | 40×40 / 4 層 / 片面実装。IP2326 充電・MP1584 5V 3A・常時オン 3.3 V LDO・電源ラッチ (D-FF)・LED レールスイッチ | **本プロジェクト** |
| **B2** | **core 基板** | **M5 AtomS3R** 実装 + BNO055 (Grove I2C) + スピーカ + **PCA9632 (I2C LED ドライバ)** + WS2812 5 系統のファンアウト | **別プロジェクト** (§6 Q1 で同定) |
| **B3** | 赤道マザーリング | ドーナツ状フラット PCB。**表裏に金パッドのみ、実装部品ゼロ** | 親 (`04-motherboard-kicad.md` 未作成) |
| **B4** | `inner_deck` × 10 | 小型 FR4。上面 = FPC 半田パッド 6 / 下面 = DIP ポゴピン 6 | 親 `01-shell-cad.md` |
| **B5** | **北極キャリア基板** (新規) | ホール素子 ×1 + 状態 LED ×2 + 抵抗 ×2 | [`docs/08`](08-north-status-window.md) |
| **B6** | 共通骨組み FPC × 10 | WS2812-2020 × 80/枚 = 計 800 | 親 `02-fpc-kicad.md` |
| — | XR-2S-30A BMS | **基板外・ワイヤ接続** (B+/BM/B−/P+/P−) | 市販品 |
| — | LiPo 2000 mAh × 2 | 2S 直列 | 市販品 |

---

## 2. 全体ブロック図

```text
                        ┌──────────── 北極 (ステータス窓) ────────────┐
                        │  B5 キャリア基板                             │
                        │   ホール素子 / LED_PWR / LED_CHG            │
                        └───────────────┬─────────────────────────────┘
                                        │ 6 本 (§4.1)   [北極 pillar Φ3mm 通路]
                                        │
  南極 磁気端子 ──CHG_IN──┐             │
   (Φ4mm 2極)             │             │
                          v             v
  ┌──────────────────────────────────────────────────────────────┐
  │  B1  電源基板 power-2S-02  (40×40 / 4層)                      │
  │                                                              │
  │   IP2326 充電 ──BATT_P/BM/BATT_N──> [XR-2S-30A BMS] ──PACK_P─┐│
  │        │ pin6 = 充電LED                                      ││
  │        └──────────────────────> LED_CHG (北極へ)             ││
  │                                                              ││
  │   HT7333-1 LDO ──> V3P3 (常時オン 3.3V / 2.5µA)              ││
  │        ├──> ホール素子 (北極)                                 ││
  │        ├──> D-FF (電源ラッチ)                                 ││
  │        └──> 各プルアップ                                      ││
  │                                                              ││
  │   HALL_OUT ─> 74LVC1G14 (Schmitt インバータ) ─> D-FF の CLK   ││
  │        ⚠️ 直結は規格違反 (docs/06 §4.3)                        ││
  │   D-FF (74LVC1G74): SD=強制ON / RD=POR+強制OFF+PWR_OFF        ││
  │        └─ FF_Q ─┬─> MP1584 の EN (3.3V 直結)                  ││
  │                 └─> LED_PWR のスイッチ                        ││
  │                                                              ││
  │   MP1584 降圧 <──SW_VIN── PACK_P ────────────────────────────┘│
  │        └──> V5V (5V / 3A)                                     │
  └────────┬──────────────────────┬──────────────────────────────┘
           │ V5V + GND            │ VETO / PWR_OFF (2 本・§4.2)
           v                      ^
  ┌──────────────────────────────────────────────────────────────┐
  │  B2  core 基板                                                │
  │    M5 AtomS3R ── GPIO01-05 ──> WS2812 5 系統                  │
  │        │       ── GPIO06 ────> スピーカ                       │
  │        │       ── I2C ───┬──> BNO055 (0x28/0x29)              │
  │        │                 └──> PCA9632 (0x62) ──> VETO/PWR_OFF │
  │        └── 3V3 ──> BNO055 / PCA9632 の電源                    │
  └────────┬──────────────────────────────────────────────────────┘
           │ 5 データ系統 + 5V + GND
           v
  ┌──────────────────────────────────────────────────────────────┐
  │  B3 赤道マザーリング (パッドのみ) ── B4 inner_deck ×10 (ポゴ6)  │
  │                            └── B6 FPC ×10 ── WS2812 × 800    │
  └──────────────────────────────────────────────────────────────┘
```

---

## 3. 電源ドメイン

| ドメイン | 電圧 | 生成元 | 供給先 | 電源 OFF 中 |
| --- | --- | --- | --- | --- |
| `PACK_P` | 6.0〜8.4 V | 2S LiPo → BMS 後 | IP2326 / MP1584 / LDO | **生きている** |
| **`V3P3`** | **3.3 V** | **HT7333-1 LDO (Iq 2.5 µA)** | ホール素子 / D-FF / プルアップ | **生きている (常時オン)** |
| `V5V` | 5 V / 最大 3 A | MP1584 (**EN 制御**) | B2 core 基板 / WS2812 × 800 | **停止** |
| AtomS3R の `3V3` | 3.3 V | AtomS3R 内部 LDO | BNO055 / **PCA9632** | **停止** (V5V 由来) |
| `LED_VCC` | 3.3 V | `V3P3` を `VETO` でゲート | 北極の LED 2 灯 | 点灯可 (充電表示) |

### オフ時電流の内訳 (設計見込み)

| 要因 | 電流 |
| --- | --- |
| MP1584 シャットダウン | 12 µA typ / 20 µA max |
| HT7333-1 LDO | 2.5 µA |
| ホール素子 (間欠動作) | 1.6〜4.0 µA |
| D-FF (LVC 静的) | ~1 µA |
| IP2326 スタンバイ | 0.7 µA |
| **PCA9632 未通電時のプルアップリーク** | **3.3〜33 µA — プルアップ値次第。§5.5 破れ ② で決着が必要** |
| **合計** | **typ 21〜23 µA / max は §5.5 の決着に依存 (41〜74 µA)** |

⚠️ **`FF_RD` のプルアップを 100 kΩ にすると max が 70 µA 級になり目標 ≤50 µA を超える。**
**§5.5 破れ ② を解決しないと予算が閉じない。**

⚠️ **D-FF / Schmitt は Nexperia 品を選ぶ** — TI の `SN74LVC1G74` は **ICC max 10 µA** で、
2 個使うと max だけで 20 µA を占める ([`docs/06`](06-power-switch.md) §5)。

⚠️ **`V3P3` が常時オンであることが設計の土台。** これがあるから電源 OFF 中でも
ホール素子と D-FF が生きており、磁石をかざした瞬間に反応できる。

---

## 4. ボード間インターフェース

### 4.1 B1 電源基板 ↔ B5 北極キャリア基板 — 6 本

[`docs/08`](08-north-status-window.md) §4.2 が正。**北極 pillar の Φ3 mm 通路**を通す (AWG30 級)。

| # | 信号 | 方向 | 備考 |
| --- | --- | --- | --- |
| 1 | `V3P3` | B1 → B5 | **ホール素子専用。veto の影響を受けてはならない** |
| 2 | `GND` | — | — |
| 3 | `HALL_OUT` | B5 → B1 | D-FF の CLK。**ツイスト + RC ローパス必須** (WS2812 データ 5 本の近傍を通る) |
| 4 | `LED_VCC` | B1 → B5 | veto でゲートされた LED アノード共通 |
| 5 | `STAT_PWR_K` | B5 → B1 | 電源 ON LED カソード戻り |
| 6 | `STAT_CHG_K` | B5 → B1 | 充電 LED カソード戻り → IP2326 pin 6 |

### 4.2 B1 電源基板 ↔ B2 core 基板

<confirmed>
**PCA9632 は B2 (core 基板) に載る** (2026-08-11 ユーザー判断)。**したがって `VETO` と `PWR_OFF` は
基板間をまたぐ信号になる。**
</confirmed>

| 信号 | 方向 | 形式 | 備考 |
| --- | --- | --- | --- |
| `V5V` | B1 → B2 | 電力 | AtomS3R + WS2812 の給電 (経路は §6 Q2) |
| `GND` | — | 電力 | — |
| **`VETO`** | **B2 → B1** | **オープンドレイン** (B1 側で 1 MΩ プルアップ) | H = 点灯 / L = 消灯 |
| **`PWR_OFF`** | **B2 → B1** | **オープンドレイン** (B1 側で 470 kΩ プルアップ) | L に引くと D-FF の CLR がクリア → 電源 OFF |

**オープンドレインで基板間を渡すのは好都合**: アサート時は低インピーダンスで GND へ引き、
非アサート時は受け側のプルアップが決めるので、**線が長くてもレベルが曖昧にならない。**

⚠️ **B2 が未通電のとき (= 電源 OFF 中)、この 2 本のプルアップ電流が
PCA9632 の ESD ダイオード経由で流れ込む。** プルアップを大きくして
**合計 約 10 µA に抑える設計** ([`docs/08`](08-north-status-window.md) §4.4b)。
**基板間配線になったことで経路は長くなるが、リーク量は変わらない。**

### 4.3 B2 core 基板 ↔ B3 マザーリング

| 信号 | 内容 |
| --- | --- |
| WS2812 データ × 5 | AtomS3R の GPIO01〜05。各系統が 160 LED (北 80 + 南 80) を駆動 |
| 5V / GND | 各カセット 6 パッド `5V-GND-DIN-DOUT-GND-5V` (回文) へ分配 |

`N_DOUT → S_DIN` のクロスはマザーリング側で行う (親 `CLAUDE.md` §2.6)。

### 4.4 B1 のコネクタ (現状の確定分)

| コネクタ | 内容 | 状態 |
| --- | --- | --- |
| `CN5` (VOUT 6p) | 5V 出力側 | **基板左右端・同一 Y・対向配置で確定**。ピン定義は §6 Q3 |
| `CN6` (VIN 6p) | 電池 / BMS 側 | 同上 |
| 充電入力 | 南極磁気端子から AWG26 × 2 | — |
| BMS 戻り | B+/BM/B−/P+/P− の 5 線 | — |
| 北極 | 6 本 (§4.1) | **新設** |
| core 基板 | `VETO` / `PWR_OFF` (+ 電力) | **新設** |

---

## 5. 電源 ON/OFF の全体シーケンス

| # | きっかけ | 起きること |
| --- | --- | --- |
| 1 | 電池挿入 | D-FF が POR でクリア → `Q`=L → MP1584 の EN=L → **OFF** |
| 2 | **北極に磁石をかざす** | ホール素子 → D-FF の CLK → トグル → `Q`=H → **EN 解放 → 5V 起動** |
| 3 | 同時に | `Q` が LED_PWR を点灯 → **AtomS3R の起動を待たずに窓が光る** |
| 4 | AtomS3R 起動 | PCA9632 は power-up 時 出力オフ → **窓は点灯のまま (boot-safe)** |
| 5 | 再生開始 | firmware が PCA9632 ch0 をシンク → `VETO` → **2 灯とも消灯** |
| 6a | もう一度かざす | D-FF トグル → `Q`=L → **5V 崩壊 → OFF** |
| 6b | firmware が電源断 | PCA9632 ch1 をシンク → `PWR_OFF` → D-FF の CLR → **OFF** |
| 7 | 充電端子を装着 | D-FF の PRE (SET) → `Q`=H → **強制 ON** (消灯中でも点く) |
| 8 | 電源 OFF のまま充電 | IP2326 が独立動作。`LED_VCC` は生きているので **充電 LED だけ点く** |

---

## 5.5 ⚠️ サブ doc 間の整合性の破れ 2 件 (2026-08-11 検出)

<impact>
[`docs/06`](06-power-switch.md) の全面改訂版と [`docs/08`](08-north-status-window.md) が
**別々に更新された結果、2 箇所で食い違っている。着手前に決着させること。**

### 破れ ① 北極コネクタが 5 極 (docs/06) vs 6 本 (docs/08)

| doc | 内容 |
| --- | --- |
| [`docs/06`](06-power-switch.md) §4.9 | `CN7` = **5 極** (`V3P3` / `GND` / `HALL_OUT` / `STAT_PWR_K` / `STAT_CHG_K`)。§4.2 で **`V3P3` が「北極 LED アノードの電源」** と定義されている |
| [`docs/08`](08-north-status-window.md) §4.2 | **6 本**。LED アノードは **`LED_VCC` (= `V3P3` を `VETO` でゲートしたもの)** |

**原因**: ユーザー確定「**動画再生中は電源 ON / 充電の 2 灯とも消灯**」を満たすには、
充電 LED も切れる必要がある。充電 LED は IP2326 pin 6 が駆動するため、
**アノード側のレールを切る以外に 2 灯を一括で消す方法がない** → `LED_VCC` が必要になり線が 1 本増える。

**提案する決着**: **docs/08 側 (6 本 + `LED_VCC`) を採用し、docs/06 の `CN7` を 6 極に改める。**
`V3P3` は**ホール素子専用**にしないと、veto 中にホール素子が止まり
**再生中に磁石をかざしても電源が切れなくなる** (docs/08 §4.2 の警告)。

### 破れ ② `FF_RD` のプルアップ値が オフ時電流の予算を壊す

| doc | 内容 |
| --- | --- |
| [`docs/06`](06-power-switch.md) §4.7 | `R5` = **100 kΩ** (V3P3 → RD) + `C6` = 100 nF で POR (τ = 10 ms) |
| [`docs/08`](08-north-status-window.md) §4.4b | PCA9632 (core 基板・**ON ドメイン**) が `FF_RD` をシンクして `PWR_OFF` |

**問題**: 電源 OFF 中は PCA9632 が未通電になる。`R5` = 100 kΩ だと
**3.3 V / 100 kΩ = 33 µA** が PCA9632 の ESD ダイオード経由で流れ込み、
docs/06 §5 の max 38〜41 µA に上乗せされて **70 µA 級 = 目標 ≤50 µA を大幅超過**する。

**提案する決着 (どちらか)**:

1. **POR の定数を `R5` = 1 MΩ / `C6` = 10 nF に変更する** (τ = 10 ms は不変)。
   リークが **3.3 µA** に下がり、合計 **41〜44 µA max** で予算内に収まる。
   ⚠️ 1 MΩ プルアップ + LVC の入力リーク ±1 µA max → 最悪 H レベル 2.3 V。
   **VIH 2.0 V は満たすが余裕は薄い** ので、入力リークの小さい Nexperia 品で確認する
2. **PCA9632 の出力を N-MOS 1 石で受ける** (docs/08 §4.4 ② の topology)。
   未通電時はゲートが pulldown で固定されリーク経路が消えるので、
   **`R5` = 100 kΩ をそのまま維持できる**。部品 +2 点

**推奨は 1 → 駄目なら 2。** どちらも「PCA9632 が未通電のときに常時オン側から
電流が流れ込まないこと」という同じ要件への対処である。
</impact>

## 6. Open questions / 未確定事項

<open_questions>
- **Q1 (ブロッカー): core 基板 (B2) の正体を同定する。**
  EasyEDA アカウントに `isolation-sphere-core40` というプロジェクトが存在する
  (project uuid `d4458b0228b14a74a004f3278b8d461d`) が、**これが B2 かどうか未確認。**
  B2 の管轄・現状・回路図の有無を確定させないと、`VETO` / `PWR_OFF` / `V5V` の
  コネクタ定義を両側で合わせられない
- **Q2: `V5V` の分配経路。** B1 → B2 → B3 か、B1 → B3 直行 + B2 へ分岐か。
  **WS2812 800 個の電流 (最大 3 A) がどの経路を通るかで銅箔幅とコネクタ定格が変わる**
- **Q3: `CN5` / `CN6` のピン定義。** 配置 (左右端・同一 Y・対向) は確定だが、
  ピンアサインは未確定 ([`docs/01`](01-legacy-analysis.md) §4)。**CN5/CN6 はユーザー担当**
- **Q4: スピーカの回路構成。** [`docs/08`](08-north-status-window.md) §4.4a の
  フォールバック (GPIO06 共用) を使う場合、アンプの有無とタップ位置の確定が必要
- **Q5: B2 に PCA9632 を載せる責任分界。** B2 は別プロジェクトなので、
  **PCA9632 の実装・I2C アドレス・ch 割り当てを B2 側の仕様として合意する必要がある**
</open_questions>

---

## 7. References

- [`CLAUDE.md`](../CLAUDE.md) — 本プロジェクトの確定仕様
- [`docs/06`](06-power-switch.md) — 電源 ON/OFF 回路 (**要全面改訂**)
- [`docs/08`](08-north-status-window.md) — 北極ステータス窓の作業指示書
- [`docs/01`](01-legacy-analysis.md) §3 / §4 — ネット名の ASCII 規約とコネクタ
- 親リポジトリ `CLAUDE.md` §2.6 / §2.7 / §2.8 — 赤道接続・BOM・極部 (**正本**)
