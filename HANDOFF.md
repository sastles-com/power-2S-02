# HANDOFF — power-2s-02 作業引き継ぎ

最終更新: 2026-08-12 (**CHARGE ブロックをピン単位で再監査 — 致命的な結線切れ 2 件を修復**)
前提知識は [`CLAUDE.md`](CLAUDE.md) を先に読むこと。

## 現在のフェーズ

**CHARGE / DCDC / PMIC の 3 ブロックは作図完了。残るのはコネクタ (ユーザー担当) とデバッグ SW 2 個。**

### CHARGE ブロックの再監査 (2026-08-12)

`charge-audit.js` (新規 skill スクリプト) で **16 部品・55 ピンを 1 ピンずつ**データシート Figure 5 と照合した。
**結線が 2 か所切れていた** — どちらも GUI ドラッグ由来で、ERC を回さない限り気付けない類:

| # | 症状 | 影響 | 対処 |
| --- | --- | --- | --- |
| ① | **`CHG_VINF` の NET_PORT が VIN 側から消え、EN (pin 12) 側の 1 個だけ残っていた** | **充電器が一切動かない** (EN が宙浮き) | VIN ノードの宙浮き端点へ NET_PORT を復元 |
| ② | **`L1.1` と LX バスの間に 25 mil の隙間** | **昇圧の主電流経路が断線** | (-60,140)–(-35,140) にワイヤを追加 |

**教訓: ネットタグ (netflag/netport) が 1 個しかないネットは「宙浮き」を意味する。**
`charge-audit.js` はこれを一目で出すので、ブロックを触った後は必ず回す。

同日あわせて実施:

- **NC フラグを 6 本**追加 (`DM` `DP` `VSET` `TIME_SET` `VIN_OVSET` `CON_SEL`) —
  `pin.setState_NoConnected(true)` は**動く** (PMIC 作業時に `U4.1` でタイムアウトしたのは一時的な事象)
- 図面の注釈 3 行を現状に合わせて更新 (CN7/CN8 は配置済み / テストポイントは配置済み / EN の扱い)
- ⚠️ **`C3` (10 µF 出力デカップリング) が `BATT_M` に乗っている** — `BATT_P` (=VOUT) が正。
  ユーザーがその一帯をレイアウト中のため**未修正** ([`docs/02`](docs/02-ip2326-module.md) §3.11 残件 ②)

⚠️ **API の引数順に注意**: `sch_PrimitiveText.create(x, y, content, ...)` — **content は第 3 引数**。
`(content, x, y)` で呼ぶと**例外も出さずに undefined を返して何も起きない**。

### 電池系コネクタを実装 (2026-08-12)

`HX PZ2.0` 系 (2.00 mm SMD 縦型、`CN7`/`CN8` と同シリーズ) で統一。→ [`docs/01`](docs/01-legacy-analysis.md) §4

| Ref | 極数 / LCSC | ピン | 備考 |
| --- | --- | --- | --- |
| `CN1` | 2P / C41417341 | GND / `CHG_IN` | **ユーザーが配置** |
| `CN2` | 3P / C41417342 | `BATT_P` / `BATT_M` / `BATT_N` | セル。**シンボルの pin2 だけ右側に出る** |
| `CN3` | 4P / C41417343 | `PACK_P`×2 / GND×2 | **放電 ~2.8 A に対し 2 A/ピンなので 2 並列** |

### 注釈の不整合を一掃 (2026-08-12)

シート内に**旧版の注釈が二重に残っていた**。以下を整理:

| 対象 | 処置 |
| --- | --- |
| 旧ディスクリートラッチの説明 7 行 (`PMIC_L`/`PMIC_H`/`PMIC_T`) | **削除** — 廃止方式で CLAUDE.md が復活を禁じている記述 |
| `SYSTEM / NET MAP` が **2 か所に重複** (x=735 の残骸 2 行 + x=1215 の旧版 14 行) | 両方削除し、**枠 (x 720..1380) の中に 18 行で作り直した** |
| `[5] Boost 5V/3A - TPS61088` / `VBAT` / `PROBE` | **削除** — 2P 版の残骸 (対応する部品も枠も無い) |
| `EN は PWR_EN (PMIC ラッチ) が駆動` | → `D-FF の Q, 3.3V CMOS が直結駆動` |
| `SW_BTN : 押しボタン (基板外)` | 削除 (外殻ボタンは廃止) → `HALL_OUT` / `VETO` の行に置き換え |
| PMIC 枠が注釈ブロック (x=-230) を含んでいなかった | 枠を左へ拡張 (topLeftX 17.5 → -240 / width 597.5 → 855) |
| CHARGE 枠が新しい注釈 2 行を含まなかった | 枠を下へ拡張 (height 400 → 435) |

### ⚠️★ 私の書き込みが取り消されている (2026-08-12)

同じページ (`1c498cb2e140475c`) で、**私が入れた変更の一部だけが消えていた**:

- 残っていた: NET_PORT の追加 / ワイヤの追加 / テキストの **create**
- 消えていた: **NC フラグ 6 本** / テキストの **modify** 2 件

ユーザーの undo (Ctrl+Z) が私の操作を巻き戻したと考えるのが自然。**API で書いた内容は EDA の undo 履歴に
乗るので、ユーザーが数回 undo すると黙って消える。** 対策:

1. **同時編集を避ける** (どちらが書くかを決めてから触る)
2. **書いた後に必ず読み返して検証する** — `charge-audit.js` と全テキストの dump を回す
3. 「消えたはず」ではなく「消えていないか」を疑う。ネットタグ 1 個 = 宙浮きのチェックが最も効く

- **2026-08-11: 電源 ON/OFF 方式をディスクリートラッチ → ホール素子 + 常時オン LDO + D-FF に全面改訂**
  (ユーザー承認済み)。設計は [`docs/06`](docs/06-power-switch.md) §4 に確定。
- **2026-08-11 (同日 後半): 旧ラッチを削除し新 PMIC の中核を作図した** → 下記「新 PMIC の作図記録」参照

### 新 PMIC の作図記録 (2026-08-11)

**削除** (ユーザー承認済み): 旧ディスクリートラッチ = **部品 21 / netport 9 / GND flag 13 / ワイヤ 18 / 注釈 9**。
枠 (rect) と `PMIC` タイトルは残した。2P 版の残骸 `[5] Boost 5V/3A - TPS61088` 注釈も同時に削除。
⚠️ **枠を跨ぐワイヤ (partial) は 0 件**だったので、他ブロックの結線は 1 本も壊れていない (削除前に実測確認)。

**新規配置 13 部品** (すべて LCSC 番号設定済み):

| Ref | 値 / 型番 | LCSC | 役割 |
| --- | --- | --- | --- |
| `U3` | HT7333-1 (SOT-89-3) | `C77777` | `PACK_P` → `V3P3` 常時オン 3.3V |
| `C22` / `C23` | 1µF | `C15849` | LDO 入出力 |
| `R19` | 1k | `C21190` | ホール入力 直列 |
| `C24` | 1nF | `C1588` | ホール入力 フィルタ (`R19·C24` = 1 µs) |
| `R20` | 100k → **DNP** | `C25803` | ホールのプルアップ。**S-5716A は CMOS 出力なので不要** → `addIntoBom=false` / `addIntoPcb=true` |
| `U4` | 74LVC1G17 (TSSOP-5) | `C3754740` | **Schmitt 非反転バッファ** |
| `U5` | **74LVC1G74DP (TSSOP-8)** | **`C3826766`** | D-FF (トグル)。**Nexperia** = §7 Q6 の要件を満たす |
| `C26` / `C27` | 100nF | `C1591` | `U4` / `U5` パスコン |
| `R21` | 470k | `C23178` | `R#D` プルアップ + POR |
| `C25` | 10nF | `C57112` | POR (`R21·C25` = **4.7 ms**) |
| `R22` | 470k | `C23178` | `S#D` プルアップ |

**ライブラリから実測したピン名** (推測していない):

| IC | ピン |
| --- | --- |
| `74LVC1G74DP` | 1=**CP** / 2=**D** / 3=**Q#** / 4=GND / 5=**Q** / 6=**R#D** / 7=**S#D** / 8=VCC |
| `74LVC1G17` | 1=NC / 2=**A** / 3=GND / 4=**Y** / 5=VCC |
| `HT7333-1` | 1=GND / 2=**IN** / 3=**OUT** |

**結線 (幾何検証済み・9 ネットすべて 1 オブジェクトに統合された)**:

| ネット | 接続 |
| --- | --- |
| `PACK_P` | netport → `U3.IN` / `C22` |
| `V3P3` | `U3.OUT` → `C23` / `U4.VCC` / `U5.VCC` / `C26` / `C27` / `R20` / `R21` / `R22` → netport |
| `HALL_OUT` | netport → `R19.1` |
| (内部) | `R19.2` → `C24` → `U4.A`、`R20` も同ノード |
| (内部) | `U4.Y` → `U5.CP` |
| (内部) | **`U5.Q#` → `U5.D`** (トグル結線) |
| (内部) | `U5.S#D` → `R22` |
| `PWR_OFF` | netport → `U5.R#D` (`R21` / `C25` も同ノード) |
| **`PWR_EN`** | `U5.Q` → netport → **`U2.EN`** (DCDC ブロックは無改造で接続) |

⚠️ **`create()` は `supplierId` に LCSC 番号ではなく型番 (`HT7333-1.1` 等) を入れる。**
配置後に `modify({supplier:"LCSC", supplierId:"Cxxxx"})` で必ず上書きすること。今回 13 点すべて修正済み
(全 49 部品が `C\d+` 形式であることを再確認した)。

⚠️ **`getAllPinsByPrimitiveId()` はピン座標と名前を返すが `net` は空。** ネット確認には使えない。
代わりに **`sch_PrimitiveWire.getAll()` の統合結果**を見る — EasyEDA は同一ネットのワイヤを
1 オブジェクトにマージするので、期待した点集合が 1 本になっていれば結線成功。

⚠️ **`modify()` に `{x, y, rotation}` だけ渡すと `supplier` / `supplierId` が消える。**
`name` (値) は残る。**再配置のたびに LCSC 番号を再設定すること。** 2026-08-11 に PMIC 13 点で再発した。

### ⚠️★ GUI で図面をドラッグすると結線が壊れる (2026-08-11 に 2 回発生)

**EasyEDA でブロックを手で動かすと、ワイヤが引きずられて別ネットと融合する / ピンから外れる。**
実際に起きた事故:

| 回 | 症状 | 発見方法 |
| --- | --- | --- |
| 1 | **`U5.GND` / `Q#` / `D` / `Q` / `R#D` / `PWR_OFF` / `PWR_EN` が 1 ネットに融合** — D-FF が丸ごと GND に落ちた | ワイヤ束の点集合を実測 |
| 2 | **`C25.2` がピンから外れて未接続** — **パワーオンリセットが機能しなくなり、電池挿入時に勝手に電源が入る可能性** | ピン単位のネット解決を実測 |
| 2 | 長さ 0 のワイヤ残骸が 7 本発生 | 同上 |

**対策: 図面を動かしたら必ずピン単位の監査を回す。** スクリプトは
`scratchpad/audit2.js` の方式 = **全ピン座標 → ワイヤ束 → 束に乗る netflag/netport 名**を辿って
`ref.pin = ネット名` の表を出す。**「見た目が繋がっている」は信用しない。**

**残っている PMIC の作図** → 「残っている作図タスク」参照

- コネクタ (CN5/CN6) は**ユーザー担当**。旧 2P 版の部品は削除済み (netport `VBUS`/`VBAT`/`VIN_B` のみ残留)

**作図環境は 2026-08-10 に Linux 機、2026-08-11 に macOS 機で疎通確認済み**
(ブリッジ ↔ EDA 接続 / Online モード / クラウド 6 プロジェクト可視)。
確認は `.claude/skills/eda-connect/scripts/eda-exec.sh .claude/skills/eda-connect/scripts/connect-check.js` 一発で済む。
warnings が「回路図ページではありません」だけなら正常。**macOS では追加設定は不要だった。**

## 完了していること

- [x] legacy `power-2S` (50×40, KiCad 10) の完全解析 — 全ネット・ブロック構成・コネクタ表を [`docs/01`](docs/01-legacy-analysis.md) に文書化
- [x] 2P 版 (EasyEDA `isolation-sphere-power`) をブリッジ経由で実測 — BOM・LCSC 番号・作図スタイル (枠 4 個 / NET_PORT 27 個 / GND 例外) を [`docs/07`](docs/07-easyeda-schematic-rules.md) に記録
- [x] 主要仕様の決定 (下表)
- [x] 仕様書一式 (`docs/01`〜`07`) 作成
- [x] Linux 作業機での作図環境の疎通確認 (2026-08-10) — 落とし穴は [`docs/07`](docs/07-easyeda-schematic-rules.md) §3 に記録
  - EasyEDA Pro 3.2.149.88089769 (2026-06-03 ビルド) / EasyEDA Pro global 版 / Online モード
  - アカウント `tajmahal.jp` (customerCode `2653466A`)、Personal team uuid `65ba7c60a1884bee825c356aebdc2ef7`
  - 複製元 2P 版 `isolation-sphere-power` = project uuid `9ead87f316b44e3b8a20dddd6de44752`
  - 同アカウントの他プロジェクト: `Isolation-sphere-BMS` / `isolation-sphere-core40` / `WS2812-square` / `WS2813-square`
- [x] **EasyEDA プロジェクト `power-2S-02` を作成** (2026-08-10) — 2P 版を GUI「名前を付けて保存」で複製
  - project uuid **`12e4820a5a9c49509b15e944859df944`** / 回路図 P1 `1c498cb2e140475c` / PCB1 `864de495483a0562`
  - 複製直後の内容: 部品 73 / 配線 76 / 枠 4 — [`docs/07`](docs/07-easyeda-schematic-rules.md) §1 の実測記録と一致
  - 複製元 `isolation-sphere-power` は無傷。**中身の改変はまだ一切していない** (TP4056/TPS61088/MAX16054 が残存)
- [x] **IP2326 の入手性確認 + データシート照合が完了** (2026-08-10) — [`docs/02`](docs/02-ip2326-module.md) を全面改訂
  - **入手可**: LCSC **C2832094** 在庫 23,110 / VQFN-24-EP(4×4) / EOL なし / **JLCPCB Extended で SMT 実装可**
    → **代替 IC の検討は不要。律速項目クリア**
  - ⚠️ **`IP2326_NPD` (C5441281) は充電電流 700 mA 未満品** — 型番が似ているので発注時に取り違え注意
  - データシート (ChipSourceTek 版 V1.2, 19p 英語) から **24 ピンの配置・全設定抵抗・標準回路 BOM** を確定
  - **充電電流 1.0 A (RISET 90 kΩ) を推奨値に** — IC 上限 1.5 A ではなく **AWG26 の ~2 A が律速** (入力 1.83 A)
  - **充電中 LED は実現可能** — IP2326 に LED 専用ピン (pin 6) があり充電中点灯/満充電消灯/異常点滅を自動でやる
    → [`docs/06`](docs/06-power-switch.md) Q4 のブロッカー解消
- [x] **充電段の残り 3 件をユーザー確認で決着** (2026-08-10) — **[`docs/02`](docs/02-ip2326-module.md) は確定 (status: 確定)**
  - **RCB は入れる。値は 120 Ω / 1206** — 標準回路の 100 Ω では `ICB < 40 mA` 制約を破るため一段上げた
  - **Type-C なし → DM/DP は未接続、CC 抵抗 5.1 kΩ×2 も不要**
  - **NTC 不使用 → RNTC 51 kΩ 1% で GND へ**
  - → **充電段 (CHARGE ブロック) は全定数が揃い、回路図を描ける状態**
- [x] **MP1584 のデータシート照合と定数設計が完了** (2026-08-10) — [`docs/03`](docs/03-mp1584-module.md) を全面改訂
  - **入手性**: **`MP1584EN-LF-Z` = LCSC `C15051`** (SOIC-8-EP、在庫 4,681、JLCPCB Extended)。
    ⚠️ **MP1584 系で在庫があるのはこれだけ** — 他 19 品目 (`MP1584EN` / `-LF-P` / `-Z` / `MP1584WN` 等) は全て在庫 0
  - **固定 5V: R1 = 105 kΩ / R2 = 20 kΩ (1%)** → 正確に 5.000 V。
    データシート標準値 (210 k/40.2 k) は無負荷ブリード電流が制約ギリギリなので採らない
  - **fsw ≈ 485 kHz (RFREQ = 200 kΩ)** — 標準回路の 100 kΩ (≈911 kHz) では
    **VIN 6.1 V 以下で 3 A の regulation ができない** (最小オフ時間 100 ns の制約)
  - **L1 = 4.7 µH / Isat ≥ 5 A / DCR ≤ 30 mΩ** (計算値 4.17 µH → 既製モジュールの 4R7 と一致)
  - ⚠️ **外付けブートストラップダイオード D2 (VOUT→BST, BAT54/1N4148) が必須** — 標準回路には無い。
    `VOUT/VIN > 65%` の高デューティ条件に該当するため ([`docs/03`](docs/03-mp1584-module.md) §3.4)
  - **補償: R3 = 100 kΩ / C3 = 220 pF / C6 なし**。**タンタルは使わず全セラミック** (ESR 零点対策)
  - **効率 89% / IC 損失 1.16〜1.28 W** を計算 → CLAUDE.md §9 の「~90%」前提を裏取り
  - **EN 仕様が判明 → [`docs/06`](docs/06-power-switch.md) の設計前提が確定** (下記)
- [x] **EasyEDA への接続と作図経路を確立 + 全部品の LCSC 選定が完了** (2026-08-11)
  - ブリッジ接続 → `power-2S-02` の回路図 P1 を開いて照合 (tabId `1c498cb2e140475c@12e4820a…`)
  - 棚卸し (`eda-schematic-dump`) が [`docs/07`](docs/07-easyeda-schematic-rules.md) §1 の記録と**完全一致**。
    差し替え対象 (MAX16054 / TP4056 / TPS61088 / SSM6J808R) も全て残存を確認。**回路図はまだ無改変**
  - ⚠️ **座標系: `create()` の Y と `getAll()` の topLeftY は符号が逆** (2 回検証)。
    docs/07 §1 の枠座標 (負値) をそのまま渡すと図面が上下反転して A4 外に出る
  - **部品配置の経路: `lib_Device.getByLcscIds()` の戻り値を `sch_PrimitiveComponent.create()` に直接渡せる**
  - **全 35 品番を解決** ([`docs/05`](docs/05-jlcpcb-fab.md) §2.1)。修正が入った値:
    - **RISET 90 kΩ → 91 kΩ (C23265)** — 90 kΩ は E24/E96 に無い標準外の値。ICHG 0.99 A
    - **充電段 R1 0.5 Ω → 1 Ω (C112305)** — 0.5 Ω は薄膜 1% 系に存在しない。降下 20 mV で無害
    - **10 µF は C2980800 → C15850** — 同 MPN だが後者は Basic で 25 V 明記
    - **22 µF は 1206 (C12891) で確定** — 0805 の 22 µF は 16 V 品しか存在しない。**基板に 6 個載る**
    - インダクタ: 充電段 **C36500** (2.2 µH, 6.0×6.0) / 降圧段 **C36417** (4.7 µH, **8.0×8.0 = 最大部品**)
    - BST ダイオード: **C81598 (1N4148W, SOD-123F, Basic)**
  - 当初の「Isat/Idc ≥ 5 A」はデータシートの 1.5 A 充電時の値。実電流から余裕を再計算して選定した
- [x] **電源スイッチ (ディスクリートラッチ) の回路・定数・BOM が確定** (2026-08-11) — [`docs/06`](docs/06-power-switch.md) §3.6
  - 3 方式を比較し **候補 A (真ディスクリート) を採用**。単押しトグルは原理的にエッジトリガ必須で
    デバウンスに晒されるため不採用 → **UX は「押下 130 ms = ON / 長押し 1.5 s = OFF」**
  - **状態記憶は 5V 出力レール自身**。エッジ検出を使わないので**デバウンス回路が不要**
  - **電源投入時 OFF は R1 (2.2 MΩ) 1 本で保証** — 競合状態が存在しない。
    BMS 過放電カットオフ→自動復帰時の再突入ループも防げる
  - しきい値を **NPN の Vbe 基準**にしてタイマばらつきを ±20% に抑えた (MOSFET Vth なら 2.5 倍振れる)
  - **Q5/Q6 (タイマリセット) が必須**と判明 — 無いと ①軽負荷でターンオフが中断 ②OFF 直後 20 秒間再 ON 不能
  - **オフ時電流: ラッチ側は実質ゼロ (~0.1 µA)**。システム合計 13 µA typ で目標 ≤50 µA を大きく下回る
  - 部品 21 点。主要部は **Basic 部品** (2N7002 = C8545 / MMBT3904 = C20526)
  - ⚠️ **ボタン線の TVS は standoff 12 V が必須** (SMF12A = C123800)。
    一般的な `ESD9B5.0` は使えない — ボタン節点に最大 8.4 V が載るため
  - ⚠️ **Q1 の I_DSS < 100 nA @85 °C の確認**が BOM 選定時の宿題 (MP1584 の EN プルアップが 1 µA しかない)
    → **2026-08-11 の方式改訂で消滅** (EN を D-FF の Q に直結したため、下記)
- [x] **⚠️ 電源 SW 方式を全面改訂 — ディスクリートラッチ廃止、ホール素子 + LDO + D-FF へ** (2026-08-11、ユーザー承認)
  - **廃止の決定的理由**: [`docs/08`](docs/08-north-status-window.md) の確定事項「状態表示は FF の Q で駆動し、
    **かざした瞬間**に点灯 (MP1584 の soft-start すら待たない)」と、旧方式の確定事項
    「**状態記憶は 5V レール自身**」が**論理的に両立しない**。旧ラッチは 5V が無い間は記憶素子を持たない
    ([`docs/06`](docs/06-power-switch.md) §1)。旧設計は同 doc §8 付録に全文保存 (再提案禁止)
  - **新構成**: `PACK_P → HT7333-1 LDO (常時オン 3.3V) → ホール素子 (北極) → 74LVC1G14 Schmitt インバータ
    → 74LVC1G74 D-FF (トグル) → Q を MP1584 の EN へ直結`
  - ⚠️ **最大の発見: ホール出力を D-FF の CLK に直結できない。** `74LVC1G74` の入力遷移レート規格は
    **≤10 ns/V (3.3V で立上り 33 ns 以内)**。docs/08 §4.2 が提案する RC (1 kΩ + 1 nF = τ 1 µs) は
    **303 ns/V = 規格の 30 倍違反**で、多重クロックによりトグルが不定になる
    (= 磁石をかざしても ON/OFF がランダム)。**Schmitt バッファが必須**
  - **Schmitt インバータ 1 個が極性問題も同時に解決** — ホールは active-low、D-FF は positive-edge なので
    非反転だと「離した瞬間」にトグルしてしまう。反転すると「かざした瞬間」になる。
    **74LVC1G14 (反転) と 74LVC1G17 (非反転) は同じ SOT-353** なので極性が変わっても基板変更不要
  - **EN 駆動から N-MOS が消えた** — D-FF の Q は 3.3V CMOS で EN (絶対最大 6V / 閾値 1.5V / プルアップ 1 µA) に
    直結できる。**旧 Q1 の「I_DSS < 100 nA @85 °C」宿題も消滅**
  - **電源投入時 OFF** は D-FF の RD に RC (**470 kΩ + 100 nF = リセット 43.7 ms**) で保証。競合状態なし。
    プルアップ値は docs/08 §4.4b (PCA9632 未通電時のリーク削減) と揃えた
  - **基板上デバッグ SW 2 個 (強制 ON → SD / 強制 OFF → RD)** を追加 (ユーザー判断)。**外殻ボタンは廃止**。
    SD/RD は非同期レベル入力なので**バウンスしても最終状態が同じ = デバウンス不要**。
    ⚠️ **CLK に機械接点を入れてはいけない** (多重トグルする)。磁石紛失時の救済手段でもある
  - **オフ電流の裏取り**: HT7333-1 = **ISS 2.5 µA typ / 4.0 µA max、VIN 絶対最大 33 V** (データシート実測)。
    74LVC1G74 (Nexperia) = **ICC 0.1 µA typ / 4.0 µA max**。
    **システム合計 27〜29 µA typ / ⚠️ 51〜54 µA max**。**typ は目標内だが max が 50 µA を超える** —
    支配項は MP1584 20 + PCA9632 未通電リーク 10 + ホール 8 で、いずれも docs/06 の管轄外 (§7 Q13)
  - ⚠️ **`SN74LVC1G74` (TI, `C70285`) は ICC max 10 µA** — docs/08 が挙げていた品番だが、
    **Nexperia 品 (max 4 µA) を選ぶこと**。TI 品 2 個で max が 50 µA に張り付く
  - **docs/08 へ 4 件申し送り** ([`docs/06`](docs/06-power-switch.md) §6): Schmitt 必須 / 出力極性の確定 / Bop は高め / オフ電流見込みの更新
  - **加えて docs/09 との矛盾 2 件を提起** ([`docs/06`](docs/06-power-switch.md) §6): 充電検出 SET / ESP32 ソフト電源断のペンディング表記
- [x] **EasyEDA 実機の幾何検証を再実行** (2026-08-11、macOS 機) — 下記「実機検証の結果」

## 実機検証の結果 (2026-08-11)

**自前の幾何チェック (全 125 線分 × 全部品ピン) を回した。CHARGE / DCDC / PMIC すべて電気的に設計どおり。**

| 項目 | 結果 |
| --- | --- |
| ネット衝突 (1 ピンに複数ネット) | **0 件** |
| 未接続ピン | 9 件 — **全て意図的** (U1 の NC 6 ピン / `Q4.G`+`R17.1` の同座標直結 / CN6.5 は 2P 残留) |
| LCSC 番号欠落 | **0 件** (57 部品) |
| ネット別ピン数 | [`docs/01`](docs/01-legacy-analysis.md) §3 と旧 docs/06 §3.6.5a の記録に一致 |

見つかった細かい差分 (いずれも電気的には無害):

- ⚠️ **部品座標が旧 docs/06 §3.6.6 の表とほぼ全点ずれていた** (Q1 220,620→245,660 / Q5 →175,575 ほか)。
  枠も X20..595/Y320..785 → **X17.5..630 / Y380..775**。GUI で動かした際に EasyEDA が
  ラバーバンドで配線を追随させた結果。**PMIC 再作図で解消するので追いかけない**
- **ネット名が空のスタブ 4 本** — `Q1.S→GND` / `Q1.D→PWR_EN` / `Q6.S→GND` / `C21.2→GND` の 5〜10 units の短線。
  netflag/netport 側でネットが解決されるので繋がるが、**自前検証では未接続に見える**。
  部品を GUI で動かすと発生する (`create()` に `net` を渡さないのと同じ状態)
- **長さ 0 のワイヤ 10 本** — PMIC 内 3 本 (545,445 / 545,415 / 200,480) + CN5/CN6 周辺 7 本
- **project description が 2P 版のまま** (`MAX16054` と書かれている)。GUI で要修正

**再検証の手順**: `tools/eda-dump-full.js` を `eda-exec.sh` で流して JSON を取り、
全ワイヤ線分に対し線上のピンを列挙してネットを突き合わせる (`sch_PrimitivePin.getAll()` は 0 件を返すので
**`sch_PrimitiveComponent.getAllPinsByPrimitiveId()` を使う**)。**長さ 0 の線分と空ネットは捨てずに別建てで報告する。**

## 旧 PMIC の作図記録 (2026-08-11、方式改訂により再作図対象)

**EasyEDA `power-2S-02` の回路図 P1 に 3 ブロックを実装し、幾何検証をクリアした。**
(設計データはクラウド上。リポジトリには回路図ファイルを置かない — CLAUDE.md §5)

| ブロック | 枠 | 部品 | 配線 | 検証 |
| --- | --- | --- | --- | --- |
| **PMIC** (ディスクリートラッチ) | X 20..595 / Y 320..785 | 21 (Q30-Q35 / R30-R37 / C30-C34 / D30-D31) | 34 | ✅ 短絡なし / 未接続なし |
| **CHARGE** (IP2326) | X 15..700 / Y 20..300 | 18 (U10 / L10 / C12-C19 / R14-R19 / D10-D11) | 25 | ✅ 同 |
| **DCDC** (MP1584) | X 690..1400 / Y 340..700 | 16 (U20 / L20 / C20-C27 / R20-R23 / D20-D21) | 15 | ✅ 同 |

- **LCSC 番号の欠落 0 件** (全 55 部品)
- GND は **netflag (Ground-GND) を各ピンに直置き** = 計 38 個。ワイヤ不要
- ネット別ピン数 34 ネットすべて設計どおり ([`docs/01`](docs/01-legacy-analysis.md) §3.1/§3.2)
- **旧 2P 版の部品はすべて削除済み** (U1/U2/U3 と付随部品 43 個、枠 2 個)

### ⚠️ 検証は自前の幾何チェックで行っている

ネットリスト API が両方使えない (`getNetlistFile()` は falsy / `sch_Netlist.getNetlist()` は例外)。
ERC (`sch_Drc.check`) も**件数しか返さない**。そのため
**「全ワイヤ線分に対し線上のピンを列挙し、ワイヤのネット ≠ ピンのネットを検出する」**
自前チェックで短絡と未接続を確認している。
**作図を変更したら必ずこのチェックを回す**こと (手順は [`docs/07`](docs/07-easyeda-schematic-rules.md) §3)。

### Designator リナンバー (2026-08-11)

旧 2P 部品を全削除したので、ブロック番号帯 (10/20/30 番台) をやめて
**種類ごとの連番** (ブロック順 CHARGE → DCDC → PMIC) に統一した。
**55 部品を改番。重複なし / LCSC 番号の欠落なしを確認済み。**

| 種類 | CHARGE | DCDC | PMIC |
| --- | --- | --- | --- |
| IC | **U1** (IP2326) | **U2** (MP1584) | — |
| インダクタ | **L1** (2.2 µH) | **L2** (4.7 µH) | — |
| トランジスタ | — | — | **Q1〜Q6** |
| 抵抗 | **R1〜R6** | **R7〜R10** | **R11〜R18** |
| コンデンサ | **C1〜C8** | **C9〜C16** | **C17〜C21** |
| ダイオード | **D1** (SS34) | **D2** (SS34) / **D3** (1N4148W) | **D4** (TVS SMF12A) |
| LED | **LED1** (充電表示) | — | **LED2** (電源 ON) |

<details><summary>旧番号との対応 (全 55 件)</summary>

`C12-C19→C1-C8` / `C20-C27→C9-C16` / `C30-C34→C17-C21` /
`R14-R19→R1-R6` / `R20-R23→R7-R10` / `R30-R37→R11-R18` /
`Q30-Q35→Q1-Q6` / `U10→U1` / `U20→U2` / `L10→L1` / `L20→L2` /
`D10→D1` / `D20→D2` / `D21→D3` / `D30→D4` / `D11→LED1` / `D31→LED2`

**改番の手順**: `modify()` は省略フィールドを壊すので `{designator, name, supplier, supplierId}`
を毎回渡す。**現在番号の昇順に処理すれば衝突しない** (目標番号は必ず現番号より小さく、
低い番号が先に空くため)。55 呼び出しを 3 スクリプトに分割した (30 呼び出し制限、[`docs/07`](docs/07-easyeda-schematic-rules.md) §3)。

</details>

⚠️ **`PMIC` の論理 Ref ([`docs/06`](docs/06-power-switch.md) §3.6.2 の Q1〜Q6) と回路図の Q1〜Q6 が一致するようになった。**
抵抗・コンデンサは論理 Ref (R1〜R8 / C1〜C5) と回路図 (R11〜R18 / C17〜C21) で**異なる**ので注意。

### 回路図の注釈 (2026-08-11 実装)

**図面単体で動作が読めるよう、各ブロックに注釈テキストを入れた** (計 34 行、fontSize 10 太字)。
配置と内容は [`docs/07`](docs/07-easyeda-schematic-rules.md) §1。要点:

- **`SYSTEM / NET MAP` 枠を新設** (X 720..1380 / Y 10..310) —
  **`BATT_*` は基板上で `PACK_P` と繋がらず、基板外の BMS (XR-2S-30A) を経由して戻る**
  ことを明記した。これが分からないと「充電出力と放電入力が繋がっていない」ように見える
- CHARGE: **NC ピンが意図的であること** (`CON_SEL` 開放=2S / `VSET` NC=8.4V 等) と
  R6=51k (NTC 未使用処理) / R3=120R (RCB) の注意書き
- DCDC: **D3 外付け BST ダイオードが必須な理由** / fsw を 485 kHz にした理由 / EN 絶対最大 6V
- PMIC: UX (130 ms / 1.5 s) と**動作シーケンス (1)〜(4)** / Q5・Q6 の役割

旧 2P 版の注釈 (`[5] Boost 5V/3A - TPS61088…`) と size-5 の `GND`/`VBAT` ラベルは削除済み。

### KiCad バックアップ (2026-08-11)

**設計データはクラウドのみ (CLAUDE.md §5) だったので、テキスト形式のスナップショットを
リポジトリに置けるようにした。** 手順と限界は [`kicad/README.md`](kicad/README.md)。

```bash
.claude/skills/eda-connect/scripts/eda-exec.sh tools/eda-dump-full.js > tools/schematic-dump.json
python3 tools/eda2kicad.py   tools/schematic-dump.json kicad/power-2S-02.kicad_sch
python3 tools/verify_kicad.py tools/schematic-dump.json kicad/power-2S-02.kicad_sch   # ← 毎回これを確認
```

**検証: 部品ピン 154 / ネット 48 が EasyEDA と完全一致** ✅
(両方を同じ幾何ルールで解いてネット分割を比較。**KiCad を起動せずに確認できる**)

- 形式は **KiCad 10** (`version 20260306`)。`legacy/power-2S.kicad_sch` を構文リファレンスにした
- **単一シート**で維持する (2026-08-11 決定)。ブロックは破線の枠で区切る。
  分割が要るのは A4 印刷が必要になった場合。グローバルラベルはシートを跨いで
  機能するので後から分割しても現状のラベルがそのまま使える
- UUID は決定論的なので**同じ入力なら同じ出力** = git の差分が安定する
- ⚠️ **フットプリント未割り当て / ERC 用 PWR_FLAG なし** — このままでは PCB は作れない。
  バックアップ目的と割り切っている

### 残っている作図タスク

- [x] ~~**PMIC 出力段 (ステータス LED 駆動 + veto)**~~ **完了 (2026-08-11)** — `Q1` + `R23` 1M + `C28` 1nF
- [x] ~~**`CN7` 北極コネクタ**~~ **完了 (2026-08-11)** — **north の `J1`/`J2` に合わせ 2 個に分割**:
  - **`CN7` = 2P (`HX PZ2.0-1x2P TP-YQ` / `C41417341`)** — 1=`GND` / 2=`V3P3`
  - **`CN8` = 4P (`HX PZ2.0-1x4P TP-YQ` / `C41417343`)** — 1=`LED_K` / 2=`HALL_OUT` / 3=`CHG_LED` / 4=`PWR_EN`
  - **2.00 mm ピッチ**で north 側と統一。**ピン順も 1:1 なのでストレートハーネスで済む**
  - ⚠️ 従来「`CN8` = core 基板コネクタ」としていた予定番号は **`CN9` 以降へ繰り下げ**
- [x] ~~**`VETO` / `PWR_OFF` 入力に 1nF フィルタ**~~ **不要だった** — `C28` (1nF) と `C25` (10nF) が兼務している
- [ ] **★テストポイント 6 個** (`V3P3` / `HALL_OUT` / `VETO` / `PWR_OFF` / `PWR_EN` / `LED_K`) — **配置中**。
  部品は **`Test-Point` (フットプリント `Test-Point-0.5mm`、LCSC 番号なしの裸パッド)** を使い
  **`addIntoBom = false` / `addIntoPcb = true`** にする。⚠️ **`CHG_LED` も追加するか要確認**
- [ ] **★デバッグ SW 2 個** (`S#D` = 強制 ON / `R#D` = 強制 OFF) — 品番未確定 ([`docs/06`](docs/06-power-switch.md) §7 Q7)。**PMIC で唯一の回路上の残作業**
- [ ] `U4.1` (NC ピン) に NC フラグを立てる — 現在未接続で ERC に出る。
  **`setState_NoConnected()` は API から呼ぶと 30 秒でタイムアウトするので GUI 作業**
- [ ] ⚠️ **`VETO` / `PWR_OFF` はどちらも「L でアサート」= アクティブ Low。名前が極性を表していない**
  → `VETO_N` / `PWR_OFF_N` へのリネームを提案中 (ユーザー判断待ち。mother-ring と docs/09/10 に波及する)
- [ ] **CN5 / CN6 とコネクタ系** — **ユーザー担当** (2026-08-11)
- [ ] **`PACK_P` → `SW_VIN` の接続** (フェライトビーズ続投か 0 Ω か) — [`docs/04`](docs/04-layout-thermal.md) §7 Q2 が未確定
- [ ] **ERC の fatal 内訳確認** — API では件数しか取れないので GUI の ERC パネルで見る
- [ ] project description を 2S 版に書き換える (GUI。変更 API が無い)
- [ ] CN5/CN6 周辺に長さ 0 のワイヤが 7 本残置 (ユーザー担当領域なので触っていない)

## 次のアクション (優先順)

1. ~~**IP2326 の LCSC 入手性確認**~~ **(完了 2026-08-10 — 入手可、[`docs/02`](docs/02-ip2326-module.md) §0)**
2. ~~**データシート取得と定数計算**~~ **(IC 2 個とも完了 2026-08-10)**
   - [x] IP2326 → [`docs/02`](docs/02-ip2326-module.md) §2/§3
   - [x] MP1584 → [`docs/03`](docs/03-mp1584-module.md) §2/§3
   - **残るのは受動部品の LCSC 品番選定 (下の 5 に統合)**
3. ~~**ディスクリートラッチ回路の確定**~~ → **方式ごと廃止。ホール + LDO + D-FF に全面改訂 (完了 2026-08-11 — [`docs/06`](docs/06-power-switch.md) §4)**
4. ~~EasyEDA で 2P 版を複製 → CHARGE / DCDC を作図~~ **(完了 2026-08-11)**
5. ~~**部品選定 (旧構成)**~~ **(完了 2026-08-11 — [`docs/05`](docs/05-jlcpcb-fab.md) §2.1 に確定 BOM 35 品番)**
6. **⚠️ ユーザー判断待ち 2 件を解消する** ← **まずこれ**
   - **[`docs/06`](docs/06-power-switch.md) §7 Q12: 「充電端子装着で強制 ON」を採用するか。**
     [`docs/09`](docs/09-system-structure.md) §5 手順 7 の提案が同 手順 8 と
     [`docs/08`](docs/08-north-status-window.md) §8 の受入基準と両立しない ([`docs/06`](docs/06-power-switch.md) §6 矛盾 A)。
     **docs/06 は不採用として設計済み**なので、採用なら受入基準の変更 + SET のエッジ化が必要
   - **[`docs/06`](docs/06-power-switch.md) §7 Q13: オフ時電流の目標を typ 基準に読み替えるか** (max が 51〜54 µA で超過)
7. **新 PMIC の BOM 3 品番を確定する** ([`docs/06`](docs/06-power-switch.md) §7)
   - **Q6: 74LVC1G74 は Nexperia 品を選ぶ** (ICC max 4 µA)。候補 `C548408` / `C53436281`。
     ⚠️ **TI `C70285` は max 10 µA なので不利**。**在庫と JLCPCB 実装可否が未確認**
   - Q7 デバッグ SW ×2 / Q8 北極コネクタ CN7 (6 極) + CN8 / Q11 1 kΩ・470 kΩ の品番
8. **PMIC ブロックを再作図** ([`docs/06`](docs/06-power-switch.md) §4、旧 21 部品のうち **14 点削除 + 16 点新規**)
   - 作図規約 (モジュール枠 / NET_PORT / GND 例外 / 座標単位 0.01 inch) は [`docs/07`](docs/07-easyeda-schematic-rules.md) を厳守
   - **書き込み系 API を呼ぶ前に `getCurrentProjectInfo().uuid === "12e4820a…"` を必ず照合する。**
     回路図/PCB の document uuid が複製元と同一なので、照合を省くと 2P 版を壊す ([`docs/07`](docs/07-easyeda-schematic-rules.md) §3 落とし穴 ④)
   - ⚠️ **部品削除は T 字接続の巻き込み事故を起こす。削除後に全ブロックの幾何検証を回す** ([`docs/07`](docs/07-easyeda-schematic-rules.md) §3)
   - [ ] project description を 2S 版の内容に書き換える (GUI。変更 API が無い。現状 `MAX16054` と書かれている)
9. **レイアウト** — 40×40 / 4 層 / 四隅 M2 / 片面実装 ([`docs/04`](docs/04-layout-thermal.md))。
   ⚠️ **HT7333-1 の動作温度上限は 85 °C** なので MP1584 から離す ([`docs/06`](docs/06-power-switch.md) §7 Q10)
10. **ERC/DRC → JLCPCB 直発注** ([`docs/05`](docs/05-jlcpcb-fab.md))

## 判断済み事項 (再議論しない)

2026-08-10 〜 2026-08-11 決定。

| 論点 | 決定 |
| --- | --- |
| ドキュメント配置 | power-2s-02 直下に自己完結 (CLAUDE.md/HANDOFF.md/docs/) |
| **EDA** | **EasyEDA Pro に移行** (回路図 + PCB)。KiCad は legacy 参照専用に降格 |
| **作図の出発点** | **2P 版 (isolation-sphere-power) を複製して 2S 版に改造** |
| 作図規約 | モジュールを四角でくくる / モジュール内は直接配線 / モジュール間は NET_PORT / GND は例外 |
| 基板 | 40×40 mm / 4 層 / **四隅 M2 穴** / **片面実装 (Top のみ)** |
| **5V 出力** | **固定 5V / 設計最大 3 A (MP1584 維持)**。IC の引き上げは行わない |
| **電源 ON/OFF** | **ディスクリートラッチ + EN 制御**。MAX16054 と直列 P-FET は廃止 |
| 電源 SW 付帯機能 | 状態表示 LED ○ (**北極キャリア基板へ移設**) / **ESP32 ソフト電源断 = 実装する** (2026-08-11 にペンディング解除。core 基板の PCA9632 ch1 → `PWR_OFF` → D-FF の RD) / **基板上デバッグ SW 2 個 ○** (強制 ON → SD / 強制 OFF → RD) / 外殻ボタン × / ESP32 への状態通知 × |
| **電源ヘッダ配置** | **CN5 (VOUT 6p) / CN6 (VIN 6p) を基板左右端・同一 Y・対向配置** (2P 版踏襲) |
| バッテリ | 2S LiPo 2000 mAh × 2 直列 — **ユーザー確認済みで正しい** (親 doc の表記変更は不要) |
| パス表記 | **絶対パス禁止**。別 PC で作業する前提でリポジトリルート相対で書く |
| **リポジトリ** | **単独リポジトリ `sastles-com/power-2S-02` が正本**。モノレポとの二重管理は廃止 (CLAUDE.md §5) |
| 作業環境 (Linux) | EasyEDA Pro 3.2.149 + `easyeda-api` skill v1.1.3 + `run-api-gateway` v1.0.5 + **Node 22 必須** ([`docs/07`](docs/07-easyeda-schematic-rules.md) §3) |
| **EDA の使用モード** | **Online モード必須**。半離線ではクラウドプロジェクトが 0 件になり作図できない ([`docs/07`](docs/07-easyeda-schematic-rules.md) §3) |
| 起動順 | **ブリッジ → EasyEDA Pro**。逆順だと拡張が 15 秒で接続を諦める → `API Gateway → Reconnect` で復旧 |
| skill 運用 | 繰り返す操作は積極的に skill 化する (特に EasyEDA 操作、CLAUDE.md §6.1) |
| **充電入力** | **コネクタのみ** (磁気端子経由)。Type-C レセプタクルは載せない |
| コネクタ | 最小構成 4〜5 個に削減。デバッグ口はテストポイントで代用 |
| **回路定数の根拠** | **データシート標準回路 + 自前計算**。既製モジュール実物の実測は行わない |
| **充電 IC の品種** | **IP2326 標準品 = LCSC `C2832094`**。`IP2326_NPD` (700 mA 未満品) と `IP2326_8V8` (CV +0.4 V) は不採用 |
| **セル数設定** | **CON_SEL (pin 10) を未接続 (floating)** で 2S。3S は 1 kΩ to GND。*既製モジュール基板の「180 kΩ で 2S」表記は基板固有で IC 仕様ではない* |
| **充電電流** | **1.0 A (RISET = 90 kΩ 1%)**。律速は IC (1.5 A) ではなく AWG26 の許容電流 |
| **CV 充電電圧** | **8.4 V (VSET = NC)** — 4.20 V/cell |
| **セルバランス** | **使う。RCB = 120 Ω / 1206 を BM ラインに直列** (均等化電流 34 mA)。データシート標準回路の 100 Ω は `ICB<40mA` 制約を破るので採らない。**0 Ω は禁止** (内蔵 MOS がセルを短絡) |
| **DM / DP (pin 1,2)** | **未接続 (NC)、配線も引かない**。Type-C 非搭載で急速充電交渉は使わず、失敗時は 5V 充電に自動フォールバックする。保険の DNP プルダウンパッドはスペース次第 |
| **NTC** | **不使用。RNTC 51 kΩ 1% で GND へ** (データシート明記の処理)。サーミスタ非実装。**入れ忘れると充電が始まらない** |
| **入力保護閾値** | UV = 4.35 V (RUV 68 kΩ、ケーブル降下を許容) / OV = 8.75 V (NC) / タイムアウト = 24 h (NC) |
| **降圧 IC の型番** | **`MP1584EN-LF-Z` = LCSC `C15051`** (SOIC-8-EP)。**MP1584 系で在庫があるのはこれだけ**。`-Z` = T&R / `-LF` = RoHS |
| **固定 5V 分圧** | **R1 = 105 kΩ / R2 = 20 kΩ (1%)** → 正確に 5.000 V。無負荷ブリード 40 µA (要件 >20 µA) |
| **降圧のスイッチング周波数** | **≈485 kHz (RFREQ = 200 kΩ 1%)**。標準回路の 100 kΩ (≈911 kHz) は**採らない** — 最小オフ時間 100 ns により VIN 6.1 V 以下で 3 A が出せなくなる |
| **降圧のインダクタ** | **4.7 µH / Isat・Idc ≥ 5 A / DCR ≤ 30 mΩ / シールド**。リップル 0.89 A、ピーク 3.44 A (電流制限 min 4.0 A) |
| **外付け BST ダイオード** | **必須。VOUT(5V) → BST に BAT54 / 1N4148**。`VOUT/VIN > 65%` の高デューティ条件に該当するため (標準回路には無い) |
| **降圧の補償** | **R3 = 100 kΩ / C3 = 220 pF / C6 なし**。出力は**全セラミック** (タンタルを足すと ESR 零点で C6 が必要になる) |
| **入出力コンデンサ** | **22 µF / 25 V ×2 + 0.1 µF** を入力・出力それぞれ。25 V は DC バイアス減衰対策 |
| **MP1584 の EN 駆動** | **D-FF の Q (3.3 V CMOS) を直結** (2026-08-11 改訂)。EN 絶対最大 6 V に対し H = 3.2 V で余裕、L は 0 V まで確実に引き切る。**N-MOS は不要** ([`docs/06`](docs/06-power-switch.md) §4.5)。~~オープンドレインで引き下げるだけ~~ |
| **オフ時消費電流の目標** | ~~≤10 µA~~ → **≤50 µA**。**実績見込み 27〜29 µA typ / ⚠️ 51〜54 µA max (超過)**。支配項は MP1584 20 + PCA9632 未通電リーク 10 + ホール 8 で docs/06 管轄外。**typ 基準に読み替えるかは要判断** ([`docs/06`](docs/06-power-switch.md) §7 Q13) |
| **電源 SW の実装方式** | **ホール素子 (北極) + 常時オン 3.3 V LDO (HT7333-1) + D-FF (74LVC1G74) トグル + EN 直結** (2026-08-11 全面改訂)。⚠️ **ディスクリートラッチ (旧候補 A) は廃止** — docs/08 の「5V を待たずに状態確定」要件と論理的に両立しないため。LTC2954 / CD4013+CD40106 / MAX16054 の不採用は据置き |
| **電源 SW の UX** | **磁石をかざす = トグル** (2026-08-11 改訂)。~~押下 130 ms / 長押し 1.5 s~~ は廃止。**ホール IC は磁気ヒステリシス内蔵で出力がバウンスしないため、旧方式が単押しトグルを却下した理由 (エッジトリガ = デバウンス必須) が消滅した** |
| **⚠️ D-FF の CLK 前に Schmitt 必須** | `74LVC1G74` の入力遷移レート規格は **≤10 ns/V (3.3 V で立上り 33 ns 以内)**。ホール OD + プルアップや RC (1 kΩ+1 nF = 303 ns/V) は**規格の 30 倍違反**で多重クロックによりトグルが不定になる。**74LVC1G14 (Schmitt インバータ、`C4469919`) を挟む**。インバータにすることで**極性問題 (active-low ホール × positive-edge FF) も同時に解決** ([`docs/06`](docs/06-power-switch.md) §4.3/§4.4) |
| **電源投入時 OFF** | **D-FF の RD に RC (470 kΩ + 100 nF = リセット 43.7 ms)**。RD は **POR + `PWR_OFF` + デバッグ SW2 の 3 入力 OR** で、プルアップ値は docs/08 §4.4b と揃えて 470 kΩ |
| **デバッグ SW を CLK に入れない** | 機械接点はバウンスするので CLK では多重トグルする。**SD / RD (非同期レベル入力) に入れる**ことでデバウンス回路を不要にした |
| ~~ラッチのしきい値~~ | 旧ディスクリートラッチの項目。**方式廃止により無効** ([`docs/06`](docs/06-power-switch.md) §8 付録に記録) |
| ~~ボタン線の ESD 保護~~ | 旧方式の項目。**外殻ボタン廃止により無効**。⚠️ **SMF12A (standoff 12 V) は `HALL_OUT` (3.3 V 系) の保護には鈍いので転用しない** ([`docs/06`](docs/06-power-switch.md) §4.9) |
| 保護 | XR-2S-30A オフボード継続。IP2326 保護は不使用 |
| 発注 | JLCPCB (EasyEDA から直発注)。Fabrication Toolkit フローは廃止 |

## ユーザーに確認が必要なこと (残ブロッカー)

✅ **充電段 (IP2326) のブロッカーは 2026-08-10 に全てクローズ** — 決定内容は下の「判断済み事項」表と
[`docs/02`](docs/02-ip2326-module.md) §5 の一覧を参照。

⚠️ **電源スイッチは 2026-08-11 に方式を全面改訂したため、ブロッカーが 2 件再オープンした** (下記)。
方式・回路・定数そのものは [`docs/06`](docs/06-power-switch.md) §4 で確定している。

**⚠️ 回路設計をブロックする残ブロッカー 2 件 (方式改訂で新たに発生):**

- ~~**③ PMIC 出力段の topology — docs/08 の内部矛盾**~~ → **✅ 解決 (2026-08-11)。**
  **原因は「IP2326 pin 6 はシンク」という誤った前提**だった。
  **データシート Figure 5 (p.16) を画像でレンダリングして確認した結果、pin 6 は「ソース」**
  (`pin6 → R2 100Ω → LED → GND`)。これで §4.3 ④ の**ソース行**が採用行になり矛盾は消える。
  - **LED は共通カソード `LED_K`**。`LED_VCC` は廃止
  - **veto = `2N7002` 1 個 + 100 kΩ プルアップ = 2 点**。P-MOS + PNP 反転段は不要
  - **電源 ON LED は MOSFET 不要** — `74LVC1G74` の Q (±24 mA 駆動) から直接吐かせる
  - **充電 LED の抵抗は 100 Ω (メーカー指定値) に戻す** — pin 6 の出力電圧が未規定なので独自計算不可
  - ⚠️ **KiCad の北極回路図は LED の向きを反転する改版が必要** (共通アノード → 共通カソード)
  - **電源基板から `LED1` / `R2` を削除済み** — 重複 + pin 6 の 5 mA では 2 灯を点けられない。
    pin 6 は netport **`CHG_LED`** として `CN7` へ渡す

<details><summary>解決前の記述 (経緯の記録)</summary>

- **③ (2026-08-11) PMIC 出力段の topology — [`docs/08`](docs/08-north-status-window.md) の内部矛盾を解消する必要がある。**
  **`docs/08` §4.3 ④ と §4.4b が食い違っている:**
  - §4.3 ④ は「IP2326 pin 6 は**シンク** → LED は共通アノード `LED_VCC` → veto 素子は **P-MOS** →
    PCA9632 では P-MOS ゲートを持ち上げられないので **PNP 反転段が必要 (計 5 点)**」と書いている
  - §4.4b は「**追加の能動素子は N-MOS 1 個のみ・直結**」と書いている
  - **PCA9632 はオープンドレインでシンクしかできない**ため、**高側レール `LED_VCC` を切る構成は §4.4b のままでは成立しない**
  - **推奨する解消案**: **veto をアノード側でなくカソード側に移す。** `LED_VCC` は `V3P3` 直結にし、
    **各 LED の帰り経路に低側 N-MOS を入れる** — `LED_EN` ノード (V3P3 へ 1 MΩ プルアップ / PCA9632 ch0 がシンク) で
    2 灯まとめて消せる。電源 ON LED は **N-MOS 2 個の直列スタック** (ゲート = `PWR_EN` と `LED_EN`) で AND を作る。
    → **2N7002 × 3 + 抵抗 2 本 = 5 点**、すべて低側スイッチで PNP 反転段が不要、しきい値の不確かさに依存しない
  - ⚠️ この案を採ると **`docs/08` の「`V3P3` と `LED_VCC` を共用してはならない」という警告は前提が変わって無効になる**
    (veto がレールを切らなくなるため、ホール素子が veto の影響を受ける経路が存在しない)

</details>

- **[`docs/06`](docs/06-power-switch.md) §7 Q12: 「充電端子装着で強制 ON」を採用するか。**
  [`docs/09`](docs/09-system-structure.md) §5 手順 7 が D-FF の PRE (SET) に充電検出を割り当てているが、
  **同 手順 8 と [`docs/08`](docs/08-north-status-window.md) §8 の受入基準「電源 OFF のまま充電すると充電 LED だけが点く」と両立しない。**
  さらに SET がレベルでアサートされ続けると**充電中に OFF 操作ができなくなる** (SD/RD 同時 Low = 不定)。
  **docs/06 は不採用として設計済み。** 採用するなら受入基準の変更 + SET のエッジ化 (ワンショット ~4 点) が必要
- **[`docs/06`](docs/06-power-switch.md) §7 Q13: オフ時電流の目標を typ 基準に読み替えるか。**
  ワーストケース積み上げが **51〜54 µA** で目標 50 µA を超える。支配項 (MP1584 20 / PCA9632 リーク 10 / ホール 8) は
  すべて docs/06 の管轄外なので、docs/06 側の努力では下げ切れない

**残ブロッカー (機構・発注側。回路設計はブロックしない):**

- ~~ボタン (SW1) の物理配置と型番~~ → **外殻ボタン廃止。基板上デバッグ SW 2 個の型番のみ** ([`docs/06`](docs/06-power-switch.md) §7 Q7)
- **北極コネクタ CN7 (6 極) / core 基板コネクタ CN8 の型番** ([`docs/06`](docs/06-power-switch.md) §7 Q8)
- **CN5 / CN6 (6p 2.54mm) のピン割り当て** — legacy J7/J8 と 2P 版で内容が異なるため 2S 版として要確定 ([`docs/01`](docs/01-legacy-analysis.md) Q3)
- **コネクタ型番** (セル/BMS 系。JST PH 系か、多極 1 個に統合するか) ([`docs/01`](docs/01-legacy-analysis.md) Q1)
- ~~**充電電流の設定値**~~ → **1.0 A (RISET 90 kΩ) を確定案に** (入力 1.83 A で AWG26 の ~2 A 内)。
  親プロジェクトとの整合のみ残る ([`docs/02`](docs/02-ip2326-module.md) §3.1)
- **レジスト色・表面処理・発注数量** ([`docs/05`](docs/05-jlcpcb-fab.md) Q2/Q3)
- 四隅 M2 穴の正確な位置と球体コア側の受け構造 ([`docs/04`](docs/04-layout-thermal.md) Q4)

## 親プロジェクトへの申し送り

1. **5V 3 A 制約**: 設計上限 3 A のうち **0.6〜0.8 A は WS2812 の静止電流**（消灯でも消費）で、
   **発光に使えるのは約 2.2 A = LED 1 個あたり平均 2.75 mA**（全白の約 5%）。
   ファーム側の輝度上限運用はこの 2.75 mA を前提に設計する必要がある (CLAUDE.md §9)。
   ※ 以前«3.75 mA» と申し送っていたが、これは静止電流を二重計上した誤り
2. **充電電流と AWG26 の整合 — 回答が出た** (2026-08-10): 親 `docs/03-power-charging.md` Q41 の
   「AWG26 安全電流 ~2 A」に合わせ、**充電電流を 1.0 A に設定** (RISET 90 kΩ)。
   このとき 5 V 入力電流は **~1.83 A** で 2 A 上限内。IC 上限の 1.5 A を使うと入力 2.74 A になり超過する。
   満充電まで約 2.5〜3 時間 (2000 mAh に対し 0.5 C)。
   ※ モジュールの謳う「15 W」は PD/QC で 9 V を引ける場合の値。本設計は Type-C を載せないので
   5 V 入力固定 = **実効 ~8.4 W** ([`docs/02`](docs/02-ip2326-module.md) §3.3)

(バッテリ構成については 2026-08-10 にユーザー確認済み。親 doc の記述変更は不要)
